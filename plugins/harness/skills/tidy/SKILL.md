---
name: tidy
description: セッションを閉じる前のお片付け。次のセッションが再開できる状態にするところまでやる — 正典(next-directions)の更新、未コミット作業のコミット、push、log への追記、配布物の世代確認。「お片付けして」「畳んで」「セッション終わるので整理して」「/harness:tidy」で発火。
---

# harness:tidy — セッションを畳む

```!
bash "${CLAUDE_SKILL_DIR}/scripts/tidy.sh"
```

`/harness:doctor` が**設定が正しいか**を見るのに対し、これは**次のセッションが再開できるか**を
見る。関心が違うので分けてある(入れる=`init` / 診る=`doctor` / 仕舞う=`tidy`)。

## やること — 見るだけでなく、片付けきる

**doctor は診断だけだが、wrap は片付けまでやる。** 上の検査結果を読み、次の順に実際に手を動かす:

1. **正典を更新する**(下記の書き方に従う。判断が要るのはここだけ)
2. **コミットする** — 作業を意味のある単位に分けて。正典の更新も含める
3. **push する** — pre-push の検証が走る。落ちたら直してから
4. 落ち穂拾い: log.md への追記、配布物の世代が古ければ `/harness:init` 再実行

**push は外向きの操作なので、実行前に何を push するか示して確認を取る**
(コミットまでは手元の操作なので、指示があれば確認なしで進めてよい)。
検証が落ちる・意図の分からない差分がある場合は、勝手に判断せずユーザーに聞く。

### 正典(next-directions.md)の更新 — 最重要

「今日コードを触ったのに正典を触っていない」が出たら、必ず書く。**次に開く自分は、
この会話を覚えていない。**

- **現在地**: 今どこにいるか。特に**未検証の危険**(動かしていない実装・仕様どおりだが
  実地未確認のもの)と**裁定待ちの判断**。概観は書かない(毎セッションのコストに見合わない)。
- **着手順**: 次にやること。理由を1行添える — 順番の根拠が無いと次のセッションが並べ替える。
- **完了の書き方**: 「何をしたか」ではなく**何を確認したか**を書く。

  ```markdown
  ~~pre-push ゲートのテンプレート化~~ ✅ 完了 — 検証: 型エラーを注入して push が
     中止されることを確認(2026-08-05, store-redirect)
  ```

  コミットハッシュだけだと、後続のエージェントが「進捗があるから終わっている」と
  読んで**勝手に完了宣言する**(Anthropic の long-running harness 記事にある観測)。
- 変化は `> **YYYY-MM-DD 更新:**` を積層。**計画は消さない。**

### 未コミットの作業

コミットするか、正典の現在地に「何をやりかけているか」を書く。どちらもしないと、
次のセッションは差分の意図が分からないまま再開することになる。

### log.md(あるリポジトリのみ)

時系列の生記録。**追記専用**で末尾へ足す。既存の記述は書き換えない
(棚卸しで正典から溶かし落とした経緯の受け皿でもある)。

### push

未 push があれば push する。ただし pre-push が走るので、検証が通らないなら
先に直すこと(`--no-verify` は緊急時だけ)。

## 関連

- **`/harness:doctor`** — 設定がベストプラクティスを守れているか(静的検査)。
- **`/harness:init`** — 配線漏れ・世代の古さを直す(冪等)。
- **`/telemetry:review`** — このセッションで何に時間を使ったかの実測。
