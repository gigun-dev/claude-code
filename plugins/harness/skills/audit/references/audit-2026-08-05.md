# 監査記録 2026-08-05(初回・当時のスナップショット)

> 「当時の状態」の記録。次回監査でライブ状態との差分を見る材料であり、現在の真実ではない。

## ベストプラクティス記事の要旨(steering-claude-code、2026-08-05 取得時点)

機構ごとのコスト/ロード時期/最適用途:

| 機構 | ロード | コスト | 最適用途 |
|---|---|---|---|
| CLAUDE.md(ルート) | セッション開始・compaction 後も再読込 | 高 | プロジェクト基盤。200行以下 |
| CLAUDE.md(サブディレクトリ) | 該当ディレクトリ操作時 | 低 | ディレクトリ固有規約 |
| Rules(paths 付き) | 該当ファイル操作時 | 低 | ファイル限定制約 |
| Skills | 呼び出し時(常時は名前+説明のみ) | 低 | 手順的ワークフロー |
| Subagents | 呼び出し時・隔離コンテキスト | 低 | 中間結果が不要な側面作業 |
| Hooks | イベント時・compaction 対象外 | 低 | 決定論的自動化 |
| Output Styles | セッション開始・最高権限 | 高 | 大規模な役割変更のみ |

アンチパターン: CLAUDE.md の「毎回Xせよ」(→Hook) / 「絶対やるな」(→Hooks/Permissions) /
30行の手順(→Skill) / paths 無し rules / プロジェクト CLAUDE.md への個人嗜好(→ユーザースコープ)。

## 当時の監査結果

- **模範解**: caldav / swift-mcp-app(ND+頭注入フック+paths 付き rules+skills+Codex アダプタ)。
  caldav の .codex/ は AGENTS.md→CLAUDE.md symlink、rules→path 別 AGENTS.md symlink、
  同一フックスクリプトの Codex SessionStart 共有という完全な二重配線だった。
- **ギャップ**: cf-asc-dashbord / tiktok-fetcher は rules 未使用でコメント方針が CLAUDE.md
  インライン常時ロード。store-redirect は設定ゼロ。コメント方針は4リポで4変種に乖離
  (系譜: cf-asc 07-05 → tiktok 07-06 → caldav 07-08 → swift-mcp-app 07-15。
  正典は https://x.com/yuki_arano/status/2067462860771623245)。
- **ユーザースコープ**: CLAUDE.md 不在。cclens 実測(953セッション・63プロジェクト)で
  edit-precondition 344件 / path-not-found 137件 / Bash の25%が単独 cd → 矯正をグローバル
  CLAUDE.md として dotfiles に新設。グローバル設定の常時コストは 0(健全)。
- **敵対的検証の教訓**: 深刻度「高」の指摘2件が偽陽性だった(stdout 注入不可説は公式 doc
  原文引用で撤回・enabledPlugins 形式は本番実績で反証)。逆に実行系検証はマーカー消失の
  フェイルオープン等の本物のバグを実行で立証した。指摘は裁定するもので従うものではない。

## cclens 実測スナップショット(2026-08-05)

- 起動時 ~16.1k tokens は全部 Claude Code 本体分、ユーザー設定分は 0
- 未使用サーフェス: LINETalkAnalyze_flutter 39個+毎セッション1.4k tokens の重いファイル、
  tdr-concierge 9個、caldav 6個、ほか17プロジェクト
- プロンプト構成: steering 5% / corrections 8%(健全な自律性バランス)
