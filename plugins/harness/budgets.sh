# 予算の正典。**この harness の閾値はここにしか無い。**
#
# ⚠️ 実行可能ではない。`source` されるためだけのファイル(shebang も無い)。
#
# 2026-08-08 に作った。それまで同じ値が2つのスクリプトに書かれており、隣に
# **「⚠️ この2つの値は … と一致していること」という散文の規律**が置かれていた ——
# 決定論的に検査できることを人間の注意力に委ねていた形で、原則3
# (予算を先に置くと規律が要らなくなる)の裏返しになっていた。
# 実際、単位の読み違い(バイト/文字)が5箇所へ伝播したのはこれが原因。
#
# **配布物(assets/session-start.sh)は実行時にこのファイルを読まない。**
# install.sh がプレースホルダを**展開して**置く —— コンパイルと同じ。だから:
#   - 配布先に実行時依存が生まれない(プラグインを消してもフックは動く)
#   - 値の正典は1箇所
#   - ドリフトが**構造的に起きない**(検知器すら要らない)
# 原則7 の Tailwind の話そのもの: **値は1箇所、表現(展開済みスクリプト)だけをその場に置く。**
#
# ⚠️ 閾値を上げて警告を消すのは禁止。棚卸し後に現況へ「下げ直す」方向のみ可
#    (上げ方向を許すと検知がラチェット式に静かに死ぬ)。

# --- next-directions.md の「頭」(SessionStart が注入する範囲) ---------------
# 文字: 切り詰め。SessionStart の stdout は 10,000 文字超で無言に切り詰められ、
#       先頭 2KB のプレビューだけが注入される(anthropics/claude-code#70460 / #84021)。
#       **ベンダー非依存で機械的な壁**なので、配布物が持つのはこれだけ。
HEAD_WARN_CHARS=8000
HEAD_HARD_CHARS=10000

# トークン: 毎セッションの実費。切り詰めに余裕があっても払い続ける。
#   ⚠️ **配布物には持たせない。**トークン数はベンダー固有(テキスト × トークナイザ)で、
#      この harness は Codex でも動く。プラグイン側(status / doctor)だけが使う。
HEAD_WARN_TOKENS=3000

# --- CLAUDE.md(無条件・毎セッション全文ロード) ------------------------------
#   ⚠️ コストは乗算される —— CLAUDE.md はサブエージェントにも継承されるが、
#      SessionStart フックは発火しない(#46696)。頭が 1 回、CLAUDE.md が
#      1 + サブエージェント起動回数。実効レバーは閾値より「置き場所」。
CLAUDE_MD_WARN_TOKENS=2000
# ⚠️ **`CLAUDE_MD_HARD_CHARS=40000` は 2026-08-08 に撤去した。**「Claude Code 本体が
#    この値で警告を出す(公式)」と書いていたが、**出典が確認できなかった** ——
#    公式 memory docs にあるのは「200行目安」だけで、「CLAUDE.md files are loaded in full
#    regardless of length」とすら書いてある。バイナリ 2.1.222 を 40,000 / 40000 / 4e4 で
#    走査してもヒットしない(同じ手法で skillListingBudgetFraction 等は取れる)。
#    **機構の裏付けが無い閾値は持たない。**残るトークン予算は自前の根拠
#    (毎セッションの実費 + サブエージェントぶんの乗算)で立つ。

# --- カタログ部 / log.md(**注入されない**ので単位は行) ----------------------
#   守っているのは「人が読み通せる長さ」で、それは実際に行で決まる。
#   **単位は機構ごとに選ぶ。揃えることが目的ではない。**
CATALOG_MAX_LINES=250
UPDATE_BLOCK_MAX=12
LOG_MAX_LINES=1500
