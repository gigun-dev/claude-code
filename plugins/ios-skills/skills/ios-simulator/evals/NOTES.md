# eval 基盤を作りながらの所見

2026-08-02。`evals/` を作った時点での観察。**実測と推測を区別して書く。**
実測は iteration-1 の2本(`with_skill` のみ)と、ファイルを読んで数えた行数・文字数だけ。
それ以外はすべて仮説であり、`run_loop.py` と `without_skill` を回すまで確定しない。

> **この文書は編集提案であって編集ではない。** SKILL.md / references / scripts には触れていない。

---

## 1. description(288文字)を読んで — トリガーしなさそうな should-trigger

### 前提: 作業中に description が入れ替わった

- 作業開始時: **744 文字**
- 作業中に親エージェントが短縮(commit `28b3a16`「長すぎた description を半分以下に短縮」)
- 現在: **288 文字**(`compatibility` フィールドが別に追加された)

`trigger_queries.json` の 20 クエリは **どちらの版にも依存しない**素朴な依頼文として書いてあるので、
そのまま両方の版を測れる。むしろ **短縮の効果を測る絶好の材料**になった。以下は 288 字版に対する読み。

### 288 字版で消えた要素

| 消えたもの | 対応する should-trigger |
|---|---|
| 操作プリミティブの列挙(スワイプ / ハードウェアボタン / ディープリンク / **テキスト入力** / アクセシビリティ走査) | t09 |
| `SIMCTL_CHILD_*` / **`simctl clone`(16.7 秒・0タップ)** / `.mobileconfig` | t05 |
| `webcal://` 回避策、**Proxyman**、「API キー」 | t08(部分)/ t20 は負例なので**改善**方向 |
| 「裏は xcrun simctl + idb」 | t10 |

### 落ちそうな should-trigger(推測。優先度順)

1. **t03「デモ動画を撮ってほしい」— 最大の穴。**
   description に「**録画**」「**動画**」の語が **744 字版にも 288 字版にも一度も無い**。
   短縮とは無関係の、元からある欠落。しかも `references/recording.md` が独立ファイルとして
   存在し、SKILL.md 本文にも録画節がある = **スキルは録画を守備範囲だと思っているのに、
   description だけがそれを言っていない**。
   iteration-1 で唯一「成果物まで到達した」run がこのタスクだったことを考えると、
   **1語を足すコストに対して見返りが大きい**。

2. **t10「idb ui tap が効かなくなった、心当たりある?」— 構造的なミスマッチ。**
   description は一貫して「**やりたいこと**」(動かして・タップして・スクショ撮って・状態を用意して)
   しか列挙していない。一方 SKILL.md 本文は、行数で見ると**半分以上が診断と誤診カタログ**
   (「★診断の規律」「★最重要ハマりどころ 1〜3」「エラー復帰の作法」「その他のハマりどころ」+
   `references/retracted-2026-08-01.md` は 452 行まるごと誤診カタログ)。
   **本文の主力が description で一切広告されていない。**
   「Simulator まわりで**うまくいかない**ときの診断」という一節が無いのは、
   overtrigger のリスクを避けた結果というより、単に書き落としに見える。

3. **t05「ログイン済みの端末を5台さっと用意できない?」**
   288 字版は「検証用の状態(…)を用意する」と抽象的に言うだけになり、
   **複数台**という語彙が消えた。「端末を量産したい」という意図から
   このスキルへ橋を架ける語が無い。744 字版には `simctl clone` があったので、
   **短縮によって明確に悪化した可能性がある候補**。ここは A/B で差が出るはず。

4. **t09「日本語打つと文字化けする」**
   288 字版で「テキスト入力」の語が消え、例が「この画面タップして」「スクショ撮って」の2つだけになった。
   これも **短縮によって悪化した可能性がある候補**。

5. **t02「iPhone で見た目確認したいんだけど」**
   末尾の「実機は対象外(ios-device-build)」が、**「iPhone」と言われたら降りる**方向に
   効きすぎる懸念。境界を切る文が、境界のこちら側まで削っていないか。

### 逆に、誤爆しそうな should-not-trigger

- **t14「XCUITest で回帰テストを書いて」** — description に「反復検証は XCUITest」が入っている。
  意図的な記述なので、誤爆したら「昇格の**判断**」と「XCUITest を**書く仕事**」の区別を
  description で付ける必要がある。
- **t19「caldav サーバーのテストが落ちてる」** — 「CalDAV」の語が残っている。
- **t20「Proxyman の設定」** — 288 字版で Proxyman が消えたので、**短縮によって改善しているはず**。
  ここで誤爆率が下がっていれば、短縮の効果を1点で示せる。

### 字数の余裕

**1024 字の上限に対して 288 字。736 字ぶん余っている。**
公式は "Keep it concise" と言うと同時に "Err on the side of being pushy — explicitly list contexts
where the skill applies" とも言っており、**短さ自体は目的ではない**。
上の 1〜4 を拾う語(録画/動画・うまくいかないときの診断・複数台・文字化け)を足しても
400 字に届かない。**短縮しすぎている可能性が高い**というのが現時点の読み。

ただしこれは全部推測。`scripts/run_loop.py` は Simulator を一切触らないので安く回せる。
**744 字版と 288 字版の両方を `--description` で指定して同じ 20 クエリに当て、
どこが良くなりどこが悪くなったかを1回で出すのが最短。**

---

## 2. SKILL.md(763 行)を読んで — 削れそうな箇所の当たり

推奨は 500 行未満。**263 行削る必要がある。**
以下は候補の列挙であって、削る作業は別のエージェントの仕事。**節ごとの実測行数つき。**

| 節(行) | 行数 | 提案 | 削減見込み |
|---|---|---|---|
| 冒頭の改訂履歴 blockquote(29〜53 相当) | 約 25 | 「全面改訂 / 第2ラウンド / 第3ラウンド」の経緯。**実行時には要らない**。`references/retracted-2026-08-01.md` の冒頭へ移し、本文は1行のポインタに | **-24** |
| `## 🔴 まっさらな Simulator では CalDAV アカウントの初回追加が必ず失敗する`(319-381) | 63 | **最大の候補。** 6 行の検証マトリクス表・「なぜ今まで気づかなかったか」・「日本の祝日が2行並ぶ」の詳細は **`references/state-provisioning.md` §4 に既に全部ある**(重複)。本文は「まっさらな端末は初回追加が必ず失敗する / 回避は `webcal://` 前置き 4 タップ / 量産は clone」の3行+リンクで足りる | **-45** |
| `### idb ui text は現在のキーボード言語を通る`(415-459) | 45 | `pbcopy` が第一選択と確定した以上、IME の挙動の詳細は二番手。切り分け表(406-414)だけ本文に残し、詳細は references へ | **-25** |
| `## その他のハマりどころ` 1・2(495-539) | 45 | スクショ出力先と `describe-all` の JSON 形状。どちらも「踏んだら調べる」類。references へ | **-32** |
| `## ワークフロー規律` / `## ジェスチャの機微` / `## エラー復帰の作法`(655-707) | 53 | Anthropic 翻案の一般論。**実測に裏付けられた本スキル固有の知見と、翻案の一般論が同じ重みで並んでいる**のは読み手の負担。圧縮して1節に | **-30** |
| `## 前提ツール`(108-133) | 26 | インストール手順。`compatibility` フィールドが追加されて内容が重複しはじめている。`references/setup.md` へ | **-20** |
| `## ★診断の規律`(80-107) | 28 | 内容は良いが、4 項目それぞれに長い実例がついている。実例を references へ移し、規律だけ残す | **-15** |
| `## アクション対応表`(601-620)と `## 典型フロー`(708-748) | 61 | 重複がある(`pbcopy` の同じコマンドが両方に出る)。対応表に寄せる | **-15** |
| `## スクリプト`(621-654) | 34 | 4 本のスクリプトの説明。`scripts/README.md` へ | **-20** |

合計 **-226 行 → 537 行**。もう一歩(`## 状態プロビジョニング` 節 35 行の圧縮など)で 500 行を切る。

### ⚠️ ただし「短くすれば直る」とは限らない

iteration-1 の `eval-caldav-account-sync` は、**スキルをロードしたのに 7 本中 6 本落ちた**。
落ちた 4 本(`Accounts3.sqlite` での判定 / 事前のアカウント有無確認 / `.mobileconfig` の効果確認 /
事前のプロキシ確認)は**すべて SKILL.md に答えが書いてある**。しかも:

- プロキシの事前チェックは **134-203 行**(本文のかなり前方)
- CalDAV 初回追加の罠は **319-381 行**(中盤)
- `.mobileconfig` が効かない話は **540-574 行**

**前方にあるのに読まれていない。** ということは、これは純粋な「長さ」の問題ではなく
**順序と形式**の問題かもしれない。「★事前チェック」は *節* として存在するが、
**手順の Step 0 にはなっていない**(`## 典型フロー` は 708 行目、本文の最後にある)。
エージェントは上から読むとは限らず、目次的に拾い読みするので、
**「最初にこれを実行しろ」が本文の 134 行目にある**構造は効きにくい。

→ 削減とセットで **「事前チェックを典型フローの Step 0 に統合し、フローを本文の先頭に置く」**
を試す価値がある。削減だけを先に入れると、この仮説が検証できなくなる
(何が効いたか分からなくなる)ので、**削減と構成変更は別イテレーションに分けたい**。

これも推測。iteration-2 で `without_skill` を取れば、
「そもそもスキル無しでも同じ失敗をするのか(= スキルの問題ではなくタスクが難しい)」が分かる。

---

## 3. eval を回す上での実務的な障害

### コスト(実測)

| | 時間 | トークン |
|---|---|---|
| eval-demo-video (with_skill) | 3,302 s ≒ **55 分** | 258,665 |
| eval-caldav-account-sync (with_skill) | 3,178 s ≒ **53 分**(※完遂ではなく打ち切り) | 203,599 |

6 本 × 2 構成 = 12 run を直列で回すと **約 11 時間・約 280 万トークン**。
**毎イテレーション全部回すのは無理。** `README.md` の「どれを回すか」で優先順位を決めてある。

### 副作用

普通のスキルの eval は「ファイルを作って捨てる」で済むが、これは違う:

- **Simulator 端末を作る**(消さないと溜まる) → `EVAL-` プレフィックス規約を決めた
- **Keychain に CA を入れる**(端末単位なので端末を消せば消える)
- **ホストのシステムプロキシ / Proxyman のルールを触る**(**ホスト全体に効く。復元必須**)
- **本番 CalDAV サーバーに書き込む**(eval 2 と 3。iteration-1 の run は自分で消して 404 まで確認していた)
- **保護対象の端末を壊しうる**(iteration-1 で実際に1件起きた)

→ 各 run のプロンプトに境界を明記する必要がある(テンプレートは `README.md`)。
これは「丁寧のため」ではなく、**iteration-1 で実害が出たから**。

### 並列に回せない

端末を分ければ `idb` の所有権は分離できるが、**システムプロキシ・`SimRenderServer`・
ホスト側のツール状態は共有資源**。iteration-1 では、他プロセスが別端末を操作している最中に
`simctl io recordVideo` が `SimRenderServer.SimulatorError` で開始に失敗している
(エージェントの推論であって確定した因果ではない)。
→ **直列。** これが上のコストをそのまま壁にしている。

### transcript が無いと採点できない(iteration-1 の最大の反省)

iteration-1 に残っているのは**エージェントの自己申告レポートだけ**。その結果:

- 「すべてのコマンドが UDID を明示しているか」という expectation を**採点できなかった**
  (`eval-caldav-account-sync` の grading.json、証拠不足のため PASS にしていない)
- ツール呼び出し回数・エラー回数(`metrics.json`)が取れず、benchmark の該当欄が 0 のまま
- 「どの reference をいつ読んだか」が自己申告に依存しており、
  **SKILL.md の到達性という一番知りたいことの証拠が弱い**

`skill-creator` の grader は `transcript_path` を前提にしている。
**iteration-2 からは `run-N/transcript.md` を必須にする。**

### eval が外部プロジェクトに依存している

6 本のうち 4 本が `swift-mcp-app` か本番 `caldav` サーバーの存在を前提にしている。
**プラグインとして他人に配布したとき、この eval は他人の手元では回らない。**
将来は `evals/files/` に最小の fixture アプリ(タップ / テキスト入力 / WebView カードだけ持つ)を
置く必要がある。ただし今は「実在するタスクで測れる」ことの方が価値が高いので、後回しでよい。

### 自己申告レポートを grading の入力にすることの限界

iteration-1 の grading は、エージェント自身が書いた反省文を読んで採点している。
**自己申告は自分の失敗に甘くはなかった**(むしろ誤操作を自分から報告している)が、
**書かれなかったことは採点できない**という非対称性がある。
公式の "Don't give the benefit of the doubt" を守ると、
**書いていない = FAIL 扱いになり、実力より低く出る**方向にバイアスがかかる。
transcript が入ったら iteration-1 のスコアと直接は比較できない点に注意
(`benchmark.json` の notes にも書いた)。

---

## 4. 今回作れなかったもの(でっち上げていないもの)

- **`benchmark.json` の `delta`** — `without_skill` が1本も無いので計算できない。
  `aggregate_benchmark.py` は baseline を 0 とみなして `+0.32` を出すが、それは
  「スキル無しなら全部 FAIL する」という未検証の主張なので **N/A に潰した**。
- **eval 3〜6 の `grading.json`** — 未実行。`evals.json` の `status_iteration_1` に「未実行」と明記。
- **トリガー eval の結果** — 未実行。上の第1節はすべて「description を読んだ推測」。


---

## 5. 共有スキルへの到達性 — subagent の制約ではないと確定した(2026-08-02 実測)

**問い**: eval 3本のうち2本が共有スキル `ios-skills:ios-simulator` を読まなかった。
これは subagent の制約か、それとも選択か。

**測り方**: 各 agent 型で subagent を立て、「ツール一覧に `Skill` はあるか / スキル一覧は
渡っているか / 名前を全部挙げよ」とだけ聞いた(ファイル読取・コマンド実行は禁止・副作用ゼロ)。

| subagent_type | agent 定義の tools | `Skill` ツール | スキル一覧 |
|---|---|---|---|
| **`claude`**(eval で使用) | `*` | **あり** | **main と同一の全 54 件**(`ios-skills:ios-simulator` を含む) |
| `implementer` | `Read, Write, Edit, Bash, Grep, Glob` | **無し** | **渡らない** |

`*` 型では渡り方も main と同じ —— system プロンプト後方の注入ブロックに
`The following skills are available for use with the Skill tool:` として並ぶ。

**結論**: 分岐点は「subagent かどうか」ではなく **agent 定義の tools に `Skill` が入っているか**。
eval 3本はすべて `claude` 型なので、**共有スキルは見えていたしロードもできた**。
読まなかったのは**能力の制約ではなく選択**であることが確定した。

### 3本の差(仮説・n=3 で未確定)

| run | 依頼文の出だし | 共有スキル |
|---|---|---|
| demo-video iter-2 | 「`~/ghq/.../swift-mcp-app` の MCPHost の…」 | ❌ Skill 呼び出し 0 件 |
| demo-video iter-3 | 同上 | ❌ `cat` でリポジトリ内スキルを読んだ |
| caldav-sync iter-2 | 「自作の CalDAV サーバー(`https://...`)に…」 | ✅ Skill で2本ロード |

**依頼がリポジトリのパスで始まると、そのリポジトリ内を探索してスキル機構を経由しない**、
という仮説と整合する。**未検証。** 検証するなら、同じ eval を「パスで始まる版」と
「目的で始まる版」で振って Skill 呼び出しの有無を比べるのが最短。

### 代償(実測)

iter-3 は共有スキルを読まなかったため、**共有スキルには書いてある罠を踏んだ** ——
`idb ui describe-all` が JSONL ではなく入れ子の JSON 配列を返す件。
プロジェクト側スキルは当該項目を共有スキルへ委譲していたので、そこには無かった。
→ **委譲は「読まれる」ことを前提にしている。読まれない経路があると穴になる。**

### ⚠️ やってはいけない検出方法

transcript に `available-skills` 等の文字列があるかを見る方法は**無効**。
**実際に Skill をロードした transcript でも同じく 0 件**になる。
正しくは **`Skill` の tool_use を直接カウント**する。
(この誤検出で一度「subagent は引けない」と誤断定した。SKILL.md「診断の規律」3
——ground truth は疑っている道具の外側で取る—— の実例。)

---

## 6. どの assertion が実際に弁別しているか(A/B の実データ・2026-08-02)

`eval-caldav-account-sync` の with/without を突き合わせた結果。
**「両構成で常に PASS する assertion は削る」を、推測ではなく実データで判定できる状態になった。**

| # | with | without | 判定 | assertion(要約) |
|---|---|---|---|---|
| 0 | ✅ | ❌ | **★弁別** | 判定を `Accounts3.sqlite` で行う(UI ではなく) |
| 1 | ✅ | ❌ | **★弁別** | 追加前にユーザー追加アカウントの有無を確認 |
| 2 | ✅ | ❌ | **★弁別** | ネットワーク検証前にシステムプロキシを確認 |
| 3 | ❌ | ❌ | 両FAIL | `Accounts3.sqlite` の前後差分(→ iteration-3 で改訂済み) |
| 4 | ✅ | ✅ | 両PASS | スクショが実在しパスが提示されている |
| 5 | ✅ | ✅ | 両PASS | 保護対象に破壊的操作なし |
| 6 | ✅ | ✅ | 両PASS | `booted` を使わず UDID 明示 |

### 判断

- **#0・#1・#2 は残す。** スキルの中核(sqlite で裏を取る / 端末の前提を疑う / 環境を先に見る)が
  そのまま出ている。**#2 は with 側が不利な環境**(プロキシ有効)で PASS しているので、
  弁別力は表の見た目より強い。
- **#4 は削除候補。** 「スクショが実在する」はタスク要件そのもので、両構成が通る。
  `_comment_scope` が予告していた「両構成で常に PASS する assertion」の実例。
  ただし **#4 を消すと成果物の実在チェックが無くなる**ので、消すのではなく
  「**採点者が画像を開いて内容を確認した**」まで要求する形に強めるほうがよい
  (iteration-1 で自己申告を信じて誤採点した件の再発防止にもなる)。
- **#5・#6 は両 PASS でも残す。** この A/B では差が出なかったが、
  **iteration-1 では両方 FAIL している**(`booted` 指定で他人の端末を撮る事故が実在)。
  安全側の assertion は「差が出ない = 不要」ではなく「差が出ない = 現状は守れている」と読む。
  退行の検出器として機能する。

### 副産物: baseline が「スキルの価値」を具体化した

without_skill はスキル記載の罠を**4つ独立に踏み直した**(詳細 `BENCHMARK.md`)。
とくに新品端末の初回追加で **総時間の 38%(約7分)** を溶かしている。
**どの記述が時間を買っているかが、推測ではなく実測で特定できた**のはこの A/B の最大の収穫。

---

## 7. 到達性の再現性 — 6 run 横断の実測(2026-08-02)

**測ったもの**: 各 run で「共有スキルを、**最初の状態変更コマンドより前に**ロードしたか」。
transcript の `Skill` tool_use と、最初の `simctl <状態変更>` / `idb ui` の**呼び出し順序**を機械的に比較。

| run | 判定 | 順序 | プロンプトの出だし |
|---|---|---|---|
| caldav-sync iter2 | ✅ 事前 | Skill#2 < 操作#6 | 「自作の CalDAV サーバー…」 |
| webview iter3 | ✅ 読んだ | Skill#1 / 操作なし | 「MCPHost のチャットに出る…」(リポジトリ名は**文末**) |
| fanout iter3 | ✅ 事前 | Skill#1 < 操作#5 | 「検証用に、…3台ほしい」 |
| japanese iter3 | ✅ 事前 | Skill#1 < 操作#3 | 「シミュレータで新規メモの…」 |
| **demo-video iter2** | ❌ **読まず** | — | **「`~/ghq/.../swift-mcp-app` の MCPHost の…」** |
| **demo-video iter3** | ❌ **読まず** | — | **同上** |

**4/6。そして失敗した2本は、どちらも「プロンプトがリポジトリのパスで始まる」run。**

### 仮説の強度

n=3 の時点で立てた「**タスクがリポジトリ名で始まると、そのリポジトリ内の `.claude/skills/` を
先に見に行く**」は、**n=6 で一度も破れていない**。
決定的なのは `webview` で、**リポジトリ名を含むが文末に置かれており、共有スキルをロードした**。
= 「リポジトリ名を含むか」ではなく「**冒頭にあるか**」で分かれている。

⚠️ **まだ因果ではない。** 検証するなら、同じ eval を
**「パスで始まる版」と「目的で始まる版」の2通り**に振って Skill 呼び出しの有無を比べるのが最短
(1本20分・2本で40分)。**交絡: demo-video は唯一 swift-mcp-app 側のタスクでもある**ので、
リポジトリの違いなのか語順の違いなのかを分離するには、
**caldav のタスクをパスで始める版**も要る(3本目)。

### 実務上の含意

- 共有スキルへの委譲は「プロジェクト側スキルにポインタを書く」だけでは**不十分**
  (iter-2 でポインタを読んだのに実行されなかった)。
- **プロンプトの語順という、スキル側からは制御できない要因**で到達性が変わる。
  → だから **agent 定義の `skills:` による事前ロード**(プロンプトに依存しない)が要る。
  `ios-skills:simulator-operator` はそのための構造だが、**リロード待ちで未検証**。
- 代償は実測済み: 共有スキルを読まなかった iter-3 は、**共有スキルには書いてある罠**
  (`describe-all` が入れ子配列を返す)を踏んでいる。

### 7-b. 測るべきは「スキルがロードされたか」ではなく「事前チェックが実行されたか」

§7 は**機構**(Skill ツールが呼ばれたか)を測っていた。だが実務で意味があるのは**振る舞い**
(環境チェックが実操作より前に走ったか)である。同じ 7 run を振る舞いで測り直した:

| run | スキル事前ロード | **事前チェック実行** |
|---|---|---|
| caldav-sync iter2 | ✅ | ✅ |
| **demo-video iter3** | **❌ 読まず** | **✅ 実行した** |
| webview iter3 | ✅ | ✅(操作なしのため不要) |
| fanout iter3 | ✅ | ✅ |
| japanese iter3 | ✅ | ✅ |
| onboarding iter3 | ✅ | ✅ |
| demo-video iter2 | ❌ | ❌ |
| | **4/6** | **6/7** |

**決定的なのは `demo-video iter3`。共有スキルを一度もロードしていないのに事前チェックを実行した。**
プロジェクト側スキル(`ios-e2e-verify`)に**チェックをコマンドごとインライン化**した効果が
実測で出ている。唯一の失敗 `iter2` は、その修正より**前**の run。

→ **到達性の問題は「共有スキルを読ませる」以外にも解ける。**
「読ませる」(agent の `skills:` 事前ロード)と「読まなくても済むようにする」
(呼ぶ側にコマンドをインライン化)は**独立した2本の対策**で、後者は既に効果が実証済み。

### 7-c. 測定バグと、そこから見つかったスキルの実在の欠陥

最初の測定では `onboarding iter3` を「❌ 順序が逆」と判定した。誤りだった ——
**`simctl create` / `bootstatus`(自分用の端末を用意する行為)を「実操作」に数えていた**ため。

しかしこの誤判定が**スキルの実在の欠陥**を露わにした:
**`sim-preflight.sh` は Booted な端末を要求する**(Shutdown だと止まる)。
なのに SKILL.md の Step 0 は「**何より先に**これを撃つ」と書いていた ——
**新規端末を作るワークフローでは物理的に不可能**。

→ 記述を訂正した。**境界は「端末を用意する」と「アプリを触る」の間**であって、
「何もしないうち」ではない。**自分の測定バグがドキュメントのバグを見つけた形。**

### 7-d. 🔴 §7 の仮説は**反証された**(2026-08-02・追加実験)

§7 で「**タスクがリポジトリのパスで始まると共有スキルを読まない**」という仮説を
n=6 で立てた。**検証したら外れた。**

**実験**: `caldav-account-sync` と同じ内容を、**リポジトリのパスで始まる文**に書き換えて実行
(「`~/ghq/github.com/gigun-dev/caldav` の CalDAV サーバーに書き込んだ予定が…」)。
→ **共有スキルを最初の simctl より前にロードした**(Skill#1 `ios-simulator`、
Skill#2 `ios-device-verification`、最初の simctl #3)。342 秒で完走。**仮説は成り立たない。**

**代替仮説「cwd かどうか」も成り立たない。** caldav(このセッションの cwd)の project skill は
スキル一覧に載るが swift-mcp-app のものは載らない、という差はある。しかし
**`onboarding iter3` は swift-mcp-app のタスクで、しかもパス始まりなのに共有スキルをロードした。**

### 現時点の正直な結論: **原因は特定できていない**

| run | リポジトリ | パス始まり | 共有スキル |
|---|---|---|---|
| caldav-sync iter2 | caldav | ✗ | ✅ |
| order-test(新規実験) | caldav | **✓** | ✅ |
| fanout / japanese | (指定なし) | ✗ | ✅ |
| webview iter3 | swift-mcp-app | ✗(文末) | ✅ |
| **onboarding iter3** | **swift-mcp-app** | **✓** | **✅** |
| **demo-video iter2 / iter3** | **swift-mcp-app** | **✓** | **❌** |

**8 run 中 6 が読み、失敗2本はどちらも `demo-video`。**
リポジトリでも語順でも説明できず、**特定の1タスクに偏っている**。

未検証の推測(**事実として扱わないこと**): demo-video は、プロジェクト側スキル
`ios-e2e-verify` が**そのタスク専用に非常に充実している**唯一のケース
(ビルド・起動・OAuth・座標系まで網羅)。十分な手引きを見つけた時点で探索を止めた、
という説明はありうるが**検証していない**。

### この反証が示す実務上の含意

**原因が特定できない = プロンプト側の工夫では潰せない。**
だからこそ **agent 定義の `skills:` による事前ロード**(呼び出し文に一切依存しない)が要る。
「読ませ方を工夫する」路線は、根拠にしていた仮説が外れた以上**当てにできない**。

一方 §7-b で示したとおり、**「読まなくても済むようにする」(呼ぶ側にコマンドをインライン化)は
効果が実証済み** —— `demo-video iter3` は共有スキルを読まずに事前チェックを実行した。
**2本の対策のうち、実証済みなのは後者だけ。**

### 7-e. 次の実験の解釈ルールを**先に**決めておく(事前登録)

§7-d で仮説が外れた。原因を推測で埋めず、**「再現するのか」だけを先に確定させる**。

**実験**: `demo-video` を**同一プロンプトで3回目**に回し、共有スキルをロードするか見る。

**解釈は実行前に固定する**(結果を見てから都合よく読まないため):

| 3回目の結果 | 結論 | 次の手 |
|---|---|---|
| **読まなかった** | demo-video で **3/3 失敗**。全体率 6/8=75% の下で3連続失敗が起きる確率は **1.6%** → **タスク固有の再現する現象**と結論してよい | そのタスクの何が違うのかを調べる価値がある |
| **読んだ** | demo-video で 2/3 失敗。**確率的ゆらぎと区別できない** | **原因追及を打ち切る。** プロンプト側の対策は諦め、`skills:` 事前ロード一本に絞る |

> **なぜ事前登録するか**: このセッションで「実測を装った推論」を複数回踏んでいる
> (110秒/タップ・動画の尺・subagent がスキルを引けない)。**結果を見てから解釈を決めると、
> どちらに転んでも説明が付いてしまう。** 先に決めておけば、その罠に落ちない。

### 7-f. 事前登録した実験の結果 — **原因追及を打ち切る**

`demo-video` を iteration-3 と**同一プロンプト**で3回目に実行した(iteration-4)。

**結果: 共有スキルを最初の端末コマンドより前にロードした**(Skill idx 2 < preflight idx 3)。
`references/recording.md` まで読んでいる。所要 734 秒(iteration-3 の 852 秒より短縮)。

**§7-e で事前に決めたルールを適用する:**

| | 値 |
|---|---|
| 全 run の共有スキルロード率 | **7/9 = 78%** |
| `demo-video` 単体 | 1/3(2/3 失敗) |
| 全体率の下で、特定タスクが3回中2回以上失敗する確率 | **0.126** |

**有意でない。確率的ゆらぎと区別できない。**
→ **事前登録どおり、到達性の原因追及を打ち切る。**

これ以上の推測(「demo-video だけプロジェクト側スキルが充実しているから」等)は
**データで支持されていないので、書かないし、対策の根拠にもしない。**

### 結論: 到達性への対策は2本、状態は非対称

| 対策 | 仕組み | 状態 |
|---|---|---|
| **呼ぶ側にコマンドをインライン化** | プロンプトにもスキルのロードにも依存しない | ✅ **実証済み**(`demo-video iter3` が共有スキル未読で事前チェックを実行) |
| **agent 定義の `skills:` 事前ロード** | 呼び出し文に一切依存しない | ⛔ **未検証**(`/reload-plugins` 待ち) |
| ~~プロンプトの書き方を工夫する~~ | ~~語順・リポジトリ名~~ | ❌ **根拠が反証された。採らない**(§7-d) |

**ロード率 78% は「制御できない残差」として受け入れ、
それに依存しない設計(上2本)で埋めるのが正しい方針**、というのが実験から出た結論。

### 7-g. 事前登録 — demo-video の「差なし」を汚染のない条件で測り直す

**問題**: §「A/B — eval-demo-video」の baseline(727s)は、**iteration-3 が整えた
DEMO-MCPHost を引き継いでいた**(`boot` も CA 投入も実行していない)。
device prep 約120秒を差し引いて「732s ≒ 727s で差なし」と結論したが、
**引き算による補正であって実測ではない。**

**実験**: 同じ依頼・手引き禁止・**既存端末の使用を全面禁止**(新規作成のみ)で回す。

**⚠️ 条件は baseline 側に不利(非対称)**。clean baseline は
`create → boot → install → CA → OAuth` を全部払う。一方 with_skill の
iter3(852s)/ iter4(734s)は **DEMO-MCPHost にアプリが既に入っていた**。
**同条件の with_skill run は存在しない。**

**解釈は実行前に固定する:**

| clean baseline の結果 | 結論 |
|---|---|
| **852s(iter3)より速い** | 条件が不利なのに速い = **demo-video でスキルの速度優位は無い**が確定。「差なし」は補正の産物ではなく本物 |
| **852s より遅い** | 差は出るが、**その差は上限値**(baseline だけが余分な install を払っているため)。**「スキルのおかげで X 秒速い」とは言えない**。言えるのは「新規端末からの通しは高い」まで |
| **20分で未完** | 完走可否そのものに差がある。ただし上と同じ理由で、**原因をスキルに帰属できない** |

> **なぜ非対称を承知で回すか**: 「差なし」の根拠が引き算だった以上、そこを実測に置き換える
> 価値がある。**ただし対称な比較にはならないので、どちらに転んでも
> 「スキルの寄与分」を数値で主張しない**ことを先に決めておく。

---

## 8. ✅ `skills:` 事前ロードの検証 — **機構は動く**(2026-08-02 実測)

セッション中ずっとブロッカーだった項目が解けた。

**方法**: `skills:` だけを持つ使い捨て agent(`tools: Read` のみ)をプロジェクト配下に作り、
**ツールを一切使わせずに**、スキル本文にしか出てこない内容を3点言わせた。

```yaml
---
name: probe-skill-preload
tools: Read
skills:
  - ios-skills:ios-simulator
---
```

**結果: `tool_uses = 0` で3問とも正答。**

| 問い | 回答 |
|---|---|
| Step 0 で最初に撃つスクリプト名 | `scripts/sim-preflight.sh` ✅ |
| `idb ui tap` の挙動で最も強調されている事実 | 「当たっても外れても無言で `exit 0`。rc=0 は証拠にならない」✅ |
| まっさらな端末に CalDAV を追加すると | 「トグル一覧が空 → データクラス0の『停止中』。回避は `webcal://` で温める」✅ |

**ファイルを一度も読んでいないのに答えられた = 起動時に本文が入っている。** 機構は動く。

### 併せて分かったこと

- **修飾名(`plugin:skill`)で解決する。** プロジェクト配下の agent 定義から
  **プラグインのスキル**を参照できた(セッション前半に「未検証」と書いた点が解消)。
- 🔴 **プロジェクト配下の agent はホットリロードされるが、プラグインの agent はされない。**
  同じタイミングで `.claude/agents/` に置いたものは使えるようになったのに、
  `plugins/ios-skills/agents/` のものは `not found` のままだった。
  **プラグイン側の反映には `/reload-plugins` が要る。**

### 到達性への含意

これで**2本の対策が揃った**:

| 対策 | 依存 | 状態 |
|---|---|---|
| 呼ぶ側にコマンドをインライン化 | なし | ✅ 実証済み(両プロジェクトに適用済み) |
| **agent の `skills:` 事前ロード** | なし | ✅ **機構を実測で検証** |
| ~~プロンプトの書き方~~ | 語順・リポジトリ名 | ❌ 反証済み・不採用 |

**共有スキルのロード率 78%(9 run)という「制御できない残差」は、
委譲先を `simulator-operator` にすれば構造的に 100% になる** —— 機構が動くことは確認した。
残るのは「実運用でその agent 経由に委譲する」という運用の徹底だけで、
**これは技術的な未検証項目ではなくなった。**

### 8-b. agent 定義は**リロード時点のスナップショット**(呼び出し時に読み直されない)

§8 の検証中に判明した。**運用上かなり効く。**

| 観測 | 結果 |
|---|---|
| 新規に置いた agent 定義(`simulator-operator`)を呼ぶ | ❌ `not found`(一覧に出ない) |
| **削除した** agent 定義(`probe-skill-preload`)を呼ぶ | ✅ **まだ呼べる**(一覧に残っている) |
| 登録済みの名前のファイルを**中身ごと差し替えて**呼ぶ | ❌ **旧定義のまま**(`tools: Read` のまま・追加した skill も入らない) |

→ **agent の追加・削除・編集は、すべてリロードするまで反映されない。**
プロジェクト配下も例外ではない(§8 で「ホットリロードされる」と書いたのは誤り ——
正しくは「**あの時点でリロードが起きた**」であって、継続的に追随するわけではない)。

**含意**: セッション中に agent 定義を書いても、そのセッションでは使えない。
**eval を agent 経由で回す検証は、リロードを挟まないと原理的に実行できない。**

### 8-c. 事前ロードの中身(2回目の検証で分かったこと)

事前ロードされたスキルは、**`<command-name>ios-skills:ios-simulator</command-name>` 付きの
ユーザーメッセージとして SKILL.md 全文が投入される**形だった
(= 自分で `Skill` ツールを呼んだときと同じ形)。`tool_uses=0` も再確認。
**機構は2回の独立した観測で検証済み。**

### 9. 事前登録 — スキル改訂は「負けた領域」を取り返せるか

**背景**: `webview-card-dark` の A/B で **baseline が with_skill を上回った**
(baseline だけが `setProperty("--color` が 0 件 = ホストテーマ未適用 と `--hairline` 未定義 を発見)。
その差分を `references/webview-offload.md` の「監査のチェックリスト」に取り込んだ。
**取り込んだが、効いたかは未検証。**

**実験**: 同じ依頼を、改訂後のスキルを使える条件で回す(iteration-4 / with_skill)。

**解釈は実行前に固定する:**

| 結果 | 結論 |
|---|---|
| **`setProperty` 0件 と `--hairline` 未定義 の両方を指摘** | ✅ **改訂が効いた。** 一度負けた領域を、**測って直して取り返せた**ことの実証。スキルは「一次情報を書く」以外でも改善しうる |
| **片方だけ** | 🔶 部分的に効いた。チェックリストの書き方をさらに詰める余地 |
| **どちらも指摘しない** | ❌ **改訂は効かなかった。** チェックリストを足しても読まれない/使われないなら、**この領域はスキルで埋められないと結論する** |

**時間は評価軸に入れない**(baseline 365s / 前回 with_skill 277s。**内容で負けたので内容で測る**)。

> **なぜこれを回すか**: ここまでの A/B は「スキルの現状を測る」ものだった。
> これは「**測って直したものが効くか**」を測る。**改善ループが閉じるかどうかの検証**であり、
> スキルの成熟そのものを問う唯一の実験。

### 9-b. 結果 — 部分的に効いた。**ただし私がこの eval を汚染した**

**事前登録の判定**: 🔶 **片方だけ = 部分的に効いた**

| 事前登録した確認項目 | 結果 |
|---|---|
| `setProperty("--color` が 0 件 = ホストテーマ未適用 | ✅ **指摘した**(配信バンドルまで検索し、`var(--color-…)` 25箇所すべてがフォールバックに落ちることまで確認) |
| `--hairline` が未定義 | ❌ **指摘しなかった**(採点者確認: 定義0・参照2 のまま実在する) |

### 🔴 しかし、この判定自体が信用できない —— 実行エージェントが汚染を申告した

> **実行エージェントの申告(原文)**: 「`webview-offload.md` のチェックリストには実例として
> 『`--text-3` はライト 2.07 / ダーク 2.51』『`#fff` on `--accent`: ライト 4.55 → ダーク 2.71』が
> **すでに書かれていました**(私はこれを計測前に読んでいます)。私の計測はこれらを再現・確認した
> ものであり、**白紙からの発見ではありません**。」

**私が改訂したとき、チェックリストに「見つけるべき答え」を実例として書き込んだ。**
その結果、**この eval はもう「発見できるか」を測れない —— 「思い出せるか」を測っている。**
自分の手で discriminating power を壊した。

### それでも「手法は転移した」と言える根拠

**スキルに書いていない領域で新しい所見が出た**(採点者が全件検証):

| 新しい所見 | 検証 |
|---|---|
| todos カードの `--pri` ライト `#ff9500` = **2.20** | ✅ 実測一致(スキルには agenda カードの例しか無い) |
| todos の `#fff` on accent: ライト **4.02** / ダーク **3.65** | ✅ 実測一致 |
| `--color-border-secondary`(カード参照)と `--color-border-primary`(ホスト送出)の**綴り違い** | ✅ 確認済み(チェックリストの「`var(--x)` の `--x` は定義されているか」を**別の変数に適用**した) |

→ **答えを覚えていただけなら、todos カードの数値は出てこない。**
**チェックリストは「方法」として機能した**と言ってよい。

### 🎓 スキル改訂の作法として一般化する

> **eval の所見をスキルに書き戻すときは、「答え」ではなく「探し方」を書く。**
> 答え(具体的な数値・具体的な変数名)を書くと、その eval は次から**再現テストにしかならない**。
>
> ただし**実務上は答えも有用**(次に読む人がすぐ直せる)。両立させるなら:
> **答えは書く。ただしその eval は discriminating eval の役目を降ろす**と記録する。

### 対応

- `eval-webview-card-dark` は **discriminating eval から外す**。
  スキルに答えが載った以上、with/without の差はもう測れない。
  **回帰テスト(既知の欠陥を見落とさないか)としてなら引き続き有効。**
- 今後この領域を測るなら、**別のカード/別の観点で新しい eval を書く**必要がある。

### 9-c. 汚染源を取り除いて eval を復帰させた

§9-b で「答えを書き込んだせいで discriminating power を失った」と結論したが、
**壊したのは自分なので直せる。**

**対応**: `references/webview-offload.md` のチェックリストから**具体的な数値と変数名を削除**し、
**探し方だけを残した**:

| 残したもの(方法) | 消したもの(答え) |
|---|---|
| 「配信バンドルまで `setProperty("--` を grep し、ホスト側が送るキー一覧と突き合わせる」 | ~~「`setProperty("--color` が 0 件だった」~~ |
| 「`var(--` の参照名を全部抽出し、**定義側との集合の差を取る**」 | ~~「`--hairline` は定義0・参照2」~~ |
| 「`background: var(--X)` と `color:#fff` が同居する規則を洗い、light/dark 両方で比を出す」 | ~~「ライト 4.55 → ダーク 2.71」~~ |
| 「両モードを同じスクリプトで測り、トークンごとに並べる」 | ~~「`--text-3` は 2.07 / 2.51」~~ |

**観点は1つ足した**(小さい文字を先に洗う)。これは答えではなく着眼点。

**⚠️ 復帰後の再検証が要る。** 方法だけで同じ所見に到達できるかを確かめるまでは、
discriminating として扱ってよいか確定していない。→ 直後に実行する。

### 9-d. 🔴 再検証は**失敗した** —— エージェントがスキルを読まなかった

**測りたかったこと**: 答えを削ったチェックリストが、**方法だけで**同じ所見に到達させられるか。

**実際に起きたこと**: **`Skill` ツール呼び出し 0 件**(採点者が transcript で確認)。
エージェント本人も「スキルは一つも起動していません。シミュレータを触らない方針だったので
`ios-simulator` は不要と判断した」と申告している。

→ **チェックリストは読まれていない。この run は改訂の効果を測っていない。実験として無効。**

### それでも得られたもの(3つ)

**① この領域でスキルが不要という証拠が 2/2 になった**

スキル無しの run が、またしても with_skill を上回った。**しかも今回が最良**:

| 発見 | baseline① | with_skill(汚染時) | **今回(スキル未読)** |
|---|---|---|---|
| ホストテーマ未適用 | ✅ | ✅ | ✅ |
| `--color-border-secondary` 綴り違い | ✅ | ✅ | ✅ |
| **`--hairline` 未定義**(ダーク 1.03:1 = 不可視) | ✅ | ❌ | ✅ |
| **`--accent-soft` が dead token** | ❌ | ❌ | ✅ **新発見** |

**② 新しい製品バグ(採点者が検証済み)**

`--accent-soft` は **定義 4 箇所・参照 0 箇所**。`--dv-hour` も **定義 1・参照 0**。
モック(`inline-preview.html:77`)には `box-shadow: 0 0 0 3px var(--accent-soft)` があるのに
実装に無い = **完了チェックの余韻リングが描かれていない**。

**③ 実行エージェントの申告の質**

「**値は再現、問題の指摘は発見**」と自分で切り分けてきた。
色の値・変数名はソースに literal にあるので再現、コントラスト比の算出と
定義/参照の突き合わせによる4件の指摘は発見、という整理。**この粒度で申告されると採点が速い。**

### 到達性の最新値

`Skill` ツールで共有スキルをロードした run: **8/11(73%)**。
この run を含めても、§7-f の結論(ゆらぎと判定・原因追及は打ち切り)は変わらない。

### 結論(この一連について)

- **チェックリストの脱汚染は完了したが、効果は未検証のまま。**
  測るには「スキルを確実に読ませた状態」が要る → **`skills:` 事前ロードの agent 経由**。
  それはリロードを挟まないと実行できない。**このセッションでは測れない。**
- **一方、この領域(コード読解・配色監査)は 2/2 でスキル不要**という結果が積み上がった。
  §「3ドメインの A/B」の結論(スキルの重心は OS レベルの状態づくり)を追加で支持する。

### 10. description を「測定で示された価値」に合わせて書き直した(2026-08-02)

**問題**: description が、**A/B で否定された価値を主張したまま**だった。

| 旧 description の主張 | 測定結果 |
|---|---|
| 「**WebView はブラウザ**(も守備範囲)」 | ❌ **スキル非依存。** 手引き無しの run が 2/2 で独立に同じ判断へ到達し、しかも**発見の質は上**だった |
| 「反復検証は XCUITest への昇格(も守備範囲)」 | 未測定。判断としては書いてあるが、**差が出るという証拠は無い** |
| 「**Simulator が絡む検証を頼まれた時点で最初に読むこと**」 | 効きが領域依存と分かった以上、**全件で読ませるのは過剰**(読む側のコストだけ増える) |

**改訂の方針**: **測定で差が出た領域を前に出し、出なかった領域を降ろす。**

- **前へ**: OS レベルのアカウント投入 / 端末を複数台そろえる / env・URL スキームでの状態注入 /
  **「まっさらな端末では OS 側の落とし穴で必ず失敗する手順があり、知らないと数分〜数十分溶ける」**
  (= 3.6倍速の差が出た、まさにその領域)。診断と録画も残す(こちらは一次情報が効く)。
- **降ろす**: 「WebView はブラウザ」「XCUITest への昇格」を**守備範囲の宣伝から外す**
  (本文と `references/` には残す —— **内容として有用だが、ロードの理由にはならない**)。
- **やめる**: 「Simulator が絡む検証を頼まれた時点で最初に読むこと」という**全件強制**。

406 文字(上限 1024)。

> **なぜ「短くする」ではなく「主張を測定に合わせる」なのか**: description の役割は
> **「読むと得をする場面を正確に告知すること」**。得をしない場面まで告知すると、
> **読み手のコストだけ増えて精度は上がらない**。今回はその「得をする場面」が
> A/B で特定できたので、そこに寄せた。
>
> ⚠️ **訂正(直後に自分で気づいた)**: 上で「クエリは旧 description を前提に書かれている」と
> 書いたが、**実物を読んだら違った。** 20 クエリはすべて**自然なユーザー依頼文**で、
> description の語彙に依存していない(「デモ動画を撮ってほしい」「日本語打つと文字化けする」等)。
>
> そして**クエリ側を書き直してはいけない**ことに気づいた。
> **trigger の expectation は「ユーザーが何を求めているか」の ground truth であって、
> description が何を宣伝しているかではない。** description を変えたから should_trigger を
> 合わせに行くと、eval は「description が description 自身と一致するか」を測るだけになる。
> **description が測られる側で、クエリは物差し。物差しを動かさない。**
>
> → 次に trigger eval を回す人へ: **旧版と新版の両方に同じ 20 クエリを当て、差分だけを見る。**
> 片方だけ測っても改訂の是非は分からない。`trigger_queries.json` に注記済み。

### 11. 事前登録 — 脱汚染したチェックリストは「方法として」機能するか(最後の未測定項目)

**背景**: §9-c で答えを削り探し方だけを残したが、§9-d の再検証はエージェントが
**スキルを読まなかった**ため測定不能だった。`skills:` 事前ロードは同一セッションで使えない。
→ **明示的に読ませて測る。** 運用経路ではないが、**「チェックリストが方法として転移するか」**は測れる。

**問い**: この領域(コード読解・配色監査)は、**スキルで改善できるのか、それとも天井なのか。**

**基準**: ベースライン(手引き無し)が到達した4件の構造バグ:
`setProperty` 未実施 / `--color-border-secondary` 綴り違い / **`--hairline` 未定義** /
**`--accent-soft` dead token**。後ろ2つは **with_skill の run が一度も見つけていない**。

**解釈は実行前に固定する:**

| 結果 | 結論 |
|---|---|
| **4件すべて** | ✅ **チェックリストは方法として機能する。** この領域は**天井ではなく、スキルで改善できる**。「スキルの価値は一次情報だけ」という結論は**修正が要る** |
| **3件(`--accent-soft` を落とす)** | 🔶 概ね機能。dead token の観点だけチェックリストに無いので、足せば届く見込み |
| **2件以下** | ❌ **転移しない。** 探し方を書いても、この領域では手引き無しに勝てない = **天井が実在する** |

⚠️ **これは運用経路の測定ではない**(明示的にロードさせている)。測っているのは
**チェックリストの中身が機能するか**だけ。到達性の問題とは切り分けて解釈する。

### 11-b. 結果 — **チェックリストは方法として転移した。この領域は天井ではない**

**事前登録の判定: 🔶 3/4 = 「概ね機能。dead token の観点を足せば届く見込み」**(中間分岐に的中)

| ベースラインが到達した構造バグ | 今回(チェックリストを読ませた) |
|---|---|
| `setProperty` 未実施 | ✅ **検出**(チェックリスト項目1から) |
| `--color-border-secondary` 綴り違い | ✅ **検出**(同項目1・ホストのキー集合と突き合わせ) |
| **`--hairline` 未定義** | ✅ **検出**(項目2・`comm -23` の差分から直接) |
| `--accent-soft` dead token | ❌ 未検出 |

**外した理由も事前に当てていた**: チェックリストには「**未定義なのに参照**」はあるが
「**定義されているのに未参照**」が無かった。**集合の差を逆向きに取るだけ**の話。
→ **項目を追加した**(逆向きの差 + モック資料との突き合わせ)。

### さらに、これまでの誰よりも良い診断が出た

🔴 **`--text-3` は light/dark の値が反転している**(採点者が検算して確認):

| token | light | dark | 極性 |
|---|---|---|---|
| `--accent` | `#2f6fed` | `#6f9cf5` | ✅ 正常(light が暗い) |
| **`--text-3`** | **`#b4b4b8`(明)** | **`#5c5c62`(暗)** | 🔴 **逆** |

ベースラインも以前の with_skill も「**両モードとも低コントラスト**」までしか言えていなかった。
今回は**原因(極性の取り違え)と直し方(値を入れ替えるだけ)**まで到達している。
→ この観点も**チェックリストに追加**した(light/dark を並べて極性を機械的に検算する)。

### 🔬 チェックリスト5項目の内訳(エージェントの申告)

**5項目すべてが所見を生んだ。空振りゼロ。** とくに:
- 「ライトも測ったか」→ **これが無ければ「ダークだけ見て問題なし」と誤結論していた**と申告
- 「同じトークンを文字色と塗りの両方に」→ 出たが**ドクトリンの想定と逆向き**(ダークではなくライトが失格)

### 汚染の申告: **すべて「発見」**

`webview-offload.md` から具体値・変数名を削除済みであることを確認したうえで、
「**カテゴリは資料が与え、インスタンスは自分で測った**」と正確に位置づけている。
「項目1が無ければ `setProperty` を grep する発想は出ず、項目2が無ければ `comm` を取らなかった」とも。

### 🎓 結論 — 自分の以前の主張を修正する

以前「**スキルの価値はモデルが自力で辿り着けない一次情報だけ**」と結論した(SKILL.md の保守ガイド)。
**これは言い過ぎだった。**

**探し方(観点のカタログ)も転移する。** 今回、チェックリスト5項目すべてが所見を生み、
そのうち2件は**項目が無ければ発想自体が出なかった**とエージェントが申告している。
一次情報でなくても、**「何を見るべきか」の列挙**は測って差が出る形で効く。

ただし条件がある: **答えではなく観点を書くこと**(§9-b で答えを書いて eval を壊した)。
**観点は転移し、答えは記憶を測るだけになる。**

### 12. 事前登録 — 改訂の効果は「自然な条件」でも出るか(最後の未測定)

§11-b の 3/4 は**明示的にスキルをロードさせた**結果で、**運用経路の測定ではない**。
自然な条件(ロードするかどうかもエージェントに委ねる)で同じ効果が出るかは未検証。

**実験**: これまでと同じ**自然な依頼文**(スキルをロードせよという指示を一切入れない)で
`webview-card-dark` を回す。自然な到達率は 78%(8/11)なので、**ロードしない可能性が 22% ある**。

**解釈は実行前に固定する:**

| 結果 | 結論 |
|---|---|
| **ロードした & 構造バグ 3件以上** | ✅ **自然な条件でも改訂は効く。** 運用経路での効果が実証された |
| **ロードした & 3件未満** | 🔶 明示ロードでは出たが自然条件では出ない = **プロンプトに押されて読んだだけ**だった疑い |
| **ロードしなかった** | ⚪ **チェックリストについては情報ゼロ**(読まれていないので当然)。ただし**到達性のデータ点が1つ増える**(8/12 or 9/12) |

⚠️ **3番目の結果になる確率が 22% ある。** その場合は「実験失敗」ではなく
**「自然な条件ではそもそも読まれないことがある」という、まさに測りたかった現実**が出ただけ。
**どちらに転んでも解釈は上の表で固定する。**

### 12-b. 結果 — 事前登録の**第3分岐**(ロードしなかった)。だが結果自体が決定的だった

**`Skill` 呼び出し 0 件**(採点者が transcript で確認。本人も「No skills invoked and no
documentation read」と申告)。→ **チェックリストについては情報ゼロ**、が事前登録どおりの判定。

**到達性: 8/12 = 67%**(前回 8/11 = 73%)。§7-f の「ゆらぎ」判定は変わらない。

### しかし、この run は「手引き無しで 3/4」だった

| この領域の「手引き無し」run | setProperty | 綴り違い | `--hairline` | `--accent-soft` | 計 |
|---|---|---|---|---|---|
| baseline①(初回 A/B) | ✅ | ✅ | ✅ | ❌ | **3/4** |
| baseline②(§9-d) | ✅ | ✅ | ✅ | ✅ | **4/4** |
| **今回(自然条件)** | ✅ | ✅ | ✅ | ❌ | **3/4** |

**3/3 の手引き無し run が 3〜4件を検出している。**
一方、**チェックリストを明示的に読ませた run も 3/4**(§11-b)。

### 🎓 これで、この領域について正確に言えること

私は2回、極端に振れた:

1. 当初:「**この領域はスキルで埋められない = 天井**」→ §11-b の 3/4 で**訂正**
2. 訂正後:「**観点のカタログも転移する。領域は広げられる**」→ **今回で、また修正が要る**

**正確な結論**:

- **チェックリストは方法として機能する**(§11-b で実証。項目が無ければ発想が出なかったと申告あり)。
  これは変わらない。
- **しかし、この領域では手引き無しでも同等に到達する**(3/3 の run が 3〜4件)。
  → **チェックリストの限界効用がほぼゼロ。** 「効く」と「差が出る」は別。
- したがって **§「3ドメインの A/B」の当初の結論(この領域では優位なし)は、
  結果としては正しかった。** ただし**理由が違う** ——
  「スキルでは埋められないから」ではなく「**モデルが元々そこまで到達するから**」。

> **一般化**: **「転移するか」と「差が出るか」を分けて考える。**
> 観点を書けば転移はする。だが**モデルが自力で同じ観点に至る領域では、書いても差は出ない。**
> スキルに書く価値があるのは、**転移し、かつモデルが自力では至らない**ものだけ。
> これが「一次情報」に見えたのは、たまたまその条件を満たすものの多くが一次情報だったから。

### 12-c. 🔴 自分が導入した交絡 — description 改訂後の到達性は **n=1 で未検証**

§7-f の統計検定は「**ロード率は一定**」を前提に「ゆらぎ」と判定した。
だが **description はセッション中に3回変わっている**。前提が崩れている可能性を検証していなかった。

実行時刻で切ると:

| 期間 | 自然条件の run | ロード率 |
|---|---|---|
| description 改訂(07:47)**前** | 10 本 | **7/10 = 70%** |
| description 改訂**後** | **1 本** | **0/1** |

**n=1 では何も言えない。** だが**唯一のデータ点が negative** であることは記録に値する。

### なぜこれがリスクなのか

改訂は **A/B の「内容」の結果**(どの領域で差が出たか)に基づいて行った。
だが **description が担うのは trigger**(いつ読まれるか)であって、内容の効きではない。
**内容の測定結果で trigger 用の文言を変えた**ことになる —— 両者は別の軸なのに。

具体的に降ろしたもの: 「WebView はブラウザ」「XCUITest への昇格」
「Simulator が絡む検証を頼まれた時点で最初に読むこと」。
**最後のものは trigger を明示的に強めていた文**で、これを外した影響は測っていない。

### 次にやること(優先度: 高)

**旧 description(4版ある)と新版に、同じ 20 クエリを当てて trigger を比較する。**
`run_loop.py --description` で版だけ差し替えられる(`trigger_queries.README.md` 参照)。
⚠️ `anthropic` パッケージと API キーが要る(このセッションでは未導入)。

**それまで、description 改訂は「内容の正直さは上がったが trigger への影響は不明」という
状態にある。** 到達性が 67% に下がったのがこの改訂のせいかどうかも、現時点では**分からない**。

### 12-d. 未検証の変更を、証拠が示す方向へ戻した

§12-c で「description 改訂は trigger への影響を測っていない」と分かった。
**測れない(API キー/リロードが要る)以上、未検証の変更を放置しない。**

**戻したもの**: 「**Simulator を実際に操作する前に読むこと**」という trigger を強める文
(理由も添えた: 環境の事前チェックを飛ばすと以降の観測が無効になる)。

**戻さなかったもの**: 「WebView はブラウザ」「XCUITest への昇格」を守備範囲として掲げる文。
**こちらは内容の A/B で明確に否定されている**(この領域では手引き無しでも同等)ので、
降ろしたままが正しい。

| 軸 | 判断根拠 | 状態 |
|---|---|---|
| **trigger**(いつ読まれるか) | 測っていない。唯一のデータ点は negative | **強める側へ戻した**(元の状態に近い) |
| **内容の正直さ**(何を守備範囲と称するか) | A/B で測定済み | **降ろしたまま** |

474 文字(上限 1024)。

> **判断の理屈**: **測っていない軸では、実績のある側に倒す。**
> 「Simulator が絡んだら読む」は 12 run 中 8 本で機能していた文言に近い。
> 一方「WebView も守備範囲」は**測って否定された**ので、こちらは戻さない。
> **2つの軸を混ぜて1度に変えたのが元の誤り**なので、軸ごとに別々に扱う。

⚠️ **これも検証されていない**(戻したことの効果も未測定)。
次に trigger eval を回すときは、**この版も含めて比較する**こと。

### 12-e. ✅ 交絡は**存在しなかった** —— description はセッション中に反映されていない

§12-c で「description をセッション中に3回変えたので、ゆらぎ判定の前提(ロード率一定)が
崩れている可能性がある」と書いた。**確かめたら、崩れていなかった。**

**方法**: subagent に**ツールを一切使わせず**、自分のコンテキストにあるスキル一覧から
`ios-skills:ios-simulator` の description を**一字一句書き写させた**。

**結果**: 書き写されたのは **02:15(c08feb6)版**。
私が **07:47(8e8cdf5)** と **08:20(b9d6b8d)** に加えた変更は**反映されていない**。

| 時刻 | 版 | 状態 |
|---|---|---|
| 01:44 | 307字 | |
| **02:15** | **427字** | **← セッションを通じてライブだったのはこれ** |
| 07:47 | 421字 | ❌ 未反映 |
| 08:20 | 486字 | ❌ 未反映 |

**全 12 本の自然条件 run は、最も早いもの(02:40)を含めて、すべて 02:15 版を見ていた。**

### したがって

- ✅ **§12-c で提起した交絡は存在しない。** ロード率は一定の前提で測られていた。
  **§7-f の「ゆらぎ」判定の前提は保たれている。**
- ✅ §12-d で「未検証だから戻した」判断も、**そもそも効果を持たない**(反映されていないので)。
  次のリロードで初めて効く。**戻したこと自体は正しいが、このセッションの数字には無関係。**
- 🔴 **skill のメタデータも、agent 定義と同じくリロード時点のスナップショット**だった
  (§8-b と同じ性質)。**セッション中にスキルを編集しても、そのセッションの測定には反映されない。**

### 🎓 一般化 — eval を回すときの必須の前提確認

**「編集したものが本当に効いているか」を、測定の前に確かめる。**
このセッションでは、**SKILL.md 本文の改訂も同様に反映されていなかった可能性が高い**
(description と同じ機構なら)。つまり §11-b の「チェックリストが効いた」という結果も、
**改訂前の本文で得られた可能性がある** ——
ただし §11-b では**エージェントが `references/webview-offload.md` をファイルとして読んでいる**
(Skill ツール経由ではなくパス指定)ので、**そちらはディスク上の最新版が読まれている**。
**本文(SKILL.md)経由の改訂と、references をファイルで読ませる経路は、反映のされ方が違う。**

→ **次のセッションでの必須手順**: スキルを編集したら、**測る前に上のプローブ**
(ツールを使わせずに description を書き写させる)**で反映を確認する。**

---

## 13. 🔬 到達性 67% を**ドメインで層別**したら、別の絵が見えた

§7-f では「demo-video が全体率から外れるか」を検定して**ゆらぎ**と判定した。
**ドメインで層別していなかった。層を無視した検定は、層構造をノイズとして扱う。**

| ドメイン | ロード率 | **A/B でのスキルの効き** |
|---|---|---|
| **caldav / 状態づくり** | **4/4 = 100%** | ✅ **3.6倍速・+3/7** |
| UI 操作 / 録画 | 2/4 = 50% | — 差なし |
| コード読解 / 配色 | 1/3 = 33% | — 差なし(baseline 同等) |
| 合計 | 7/11 = 64% | |

**スキルが効く領域では 4/4 でロードされ、効かない領域では 3/7 しかロードされていない。**

### これが正しければ、「到達性の問題」の意味が変わる

**欠陥ではなく、trigger が正しく弁別している**ことになる。
「67%」という総計は、**スキルを必要としない領域を混ぜて平均した数字**であって、
**必要な領域だけ見れば 100%**。

### ⚠️ ただし統計的には有意でない

Fisher 正確検定(片側)**p = 0.106**。n=11 では**有意水準に届かない**。
**「ランダムなゆらぎ」より説明力のある仮説**だが、**確定ではない。**

### 検証方法(次のセッションでの最優先)

**ドメインごとに run を増やす。** 各ドメイン 6〜8 本あれば決着する見込み。
特に「効かない領域」でロードされない、が再現するかが鍵。
- 効く領域(caldav/状態づくり)を **+4 本** → 8/8 なら極めて強い
- 効かない領域(コード読解)を **+3 本** → 1/6 なら弁別が確定

⚠️ **description は 02:15 版のままセッションを通じて固定**されていた(§12-e)ので、
**この 11 本は同一条件での観測**である。層別の比較として妥当。

### 🎓 教訓 — 自分の検定設計の誤り

「ゆらぎ」と判定して**原因追及を打ち切った**のは、**層別を試さないまま**だった。
**打ち切りの判断そのものが早すぎた。**
事前登録した解釈ルールは守ったが、**そのルールが検定すべき構造を見落としていた。**
→ **事前登録は「結果を見てから解釈を変えない」ためには有効だが、
「そもそも正しい仮説を検定しているか」は保証しない。**

### 13-b. 事前登録 — 層別仮説の追試(各ドメイン +1)

§13 の層別(効く領域 4/4 vs 効かない領域 3/7、p=0.106)を、**両ドメインに1本ずつ足して**検定し直す。
**測るのはロードの有無だけ**(内容の採点はしない)。依頼文は**自然な形**でスキル指示を入れない。

**解釈は実行前に固定する:**

| 結果 | 5/5 vs 3/8 の Fisher p | 結論 |
|---|---|---|
| 効く領域 ✅ / 効かない領域 ❌ | 約 0.06 | 🔶 **仮説を支持する方向。** さらに追試すれば決着する見込み |
| 効く領域 ✅ / 効かない領域 ✅ | 約 0.20 | ⚪ **弁別は弱まる。** 「効かない領域でも読む」なら trigger は弁別していない |
| 効く領域 ❌ / どちらでも | — | ❌ **仮説が崩れる。** 効く領域で 4/5 になれば「100%」は言えない |

⚠️ **n=13 でも有意水準には届かない見込み。** それでも**方向が一貫するか**は分かる。
**1本で決着するとは主張しない。**

### 13-c. ✅ 追試の結果 — **層別仮説は有意になった(p = 0.044)**

事前登録した2本(各ドメイン+1)を実行:

| 追加 run | ドメイン | Skill ロード | 予測どおりか |
|---|---|---|---|
| `EVAL-strat-a`(CalDAV 端末を2台) | **効く領域** | ✅ **ロード**(SKILL.md + `state-provisioning.md`) | ✅ |
| `.swipe-del` の配色確認 | **効かない領域** | ❌ **未ロード**(本人申告「何も参照していません」) | ✅ |

**両方とも事前登録の第1分岐に的中。**

| | 前回(n=11) | **追試後(n=13)** |
|---|---|---|
| 効く領域(caldav/状態づくり) | 4/4 | **5/5 = 100%** |
| 効かない領域(UI操作+コード読解) | 3/7 | **3/8 = 38%** |
| **Fisher 正確検定(片側)** | p = 0.106 | **p = 0.044** ✅ |

### 結論 — 「到達性 67%」は欠陥ではなく**弁別**だった

**スキルは、効く領域では 5/5 で読まれ、効かない領域では 3/8 しか読まれていない。**
総計 8/13 = 62% という数字は、**スキルを必要としない領域を混ぜて平均したもの**にすぎない。

→ **§7-f で「ゆらぎ」と判定して原因追及を打ち切ったのは誤りだった。**
原因はあった。**層別しなかったから見えなかった。**

### ⚠️ 正直に付ける留保

- **層別は事後的**(データを見てからドメインを定義した)。**p 値は楽観的に出る。**
  ただし**層の定義は A/B の効果測定(ロードとは独立のデータ)**に基づく。
- **確認の2本は事前登録済み**で、**方向を予測してから実行し、両方とも予測どおりだった。**
  ここが最も強い部分。
- n=13 は小さい。**「弁別している」を確定と呼ぶには、各層あと数本ほしい。**

### 副次: 効く領域の run はスキルの中身を実際に使っていた

`EVAL-strat-a` の run は `scripts/sim-preflight.sh`、`sim-nav.py --id com.apple.settings.apps`
→ `com.apple.mobilecal` → `ACCOUNTS`(**AXUniqueId チェーン**)、`sim-shot.sh` を使い、
`state-provisioning.md` §4・§2 に従って**手作業ではなく種からの clone** を選んでいる。
**440秒で2台。** スキルの中身が運用でそのまま使われた実例。
