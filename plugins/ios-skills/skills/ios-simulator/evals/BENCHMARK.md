# ios-simulator スキル ベンチマーク(2026-08-02 時点)

**すべて 20 分の時間上限つき。採点は自己申告に依拠せず、transcript の機械検証と
採点者による成果物の直接検査で確定させている。**

## A/B — スキルの有無(eval-caldav-account-sync)

| | スコア | 所要 | プロキシ環境 |
|---|---|---|---|
| **with_skill** | **6/7** | **305s(5分05秒)** | `Enabled: Yes`(罠が発火する) |
| without_skill | 3/7 | 1100s(18分20秒) | `Enabled: No`(罠が発火しない) |
| **差** | **+3/7** | **3.6倍速** | **without 側に有利** |

⚠️ **環境条件は without_skill 側に有利だった。** それでも 3.6 倍遅い。

### baseline の条件遵守(transcript で検証済み)

`Skill` ツール呼び出し **0 件** / `.claude/skills` 配下・`plugins/` 配下・
Simulator 手引き文書への Read/Bash アクセス **0 件**。汚染なし。
リポジトリのソース・`CLAUDE.md`・`Makefile`・`--help`・Web 検索は許可した。

### baseline は目標に到達した —— ここが重要

**「スキルが無いとできない」ではない。** 有能なモデルは手引き無しでも 18 分で到達する。
スキルが買っているのは **到達可否ではなく、時間と証拠の質**:

- **時間**: 305s vs 1100s
- **証拠の質**: without 側は判定を**設定アプリの見た目**で行った(`Accounts3.sqlite` を
  一度も見ていない)。「停止中」の文字を読んで気づけたが、**UI が嘘をつく場面では破綻する**。

### baseline が「自力で再発見」したもの(= スキルの記述が実際に時間を買っている証拠)

baseline は手引き無しで、**スキルに書いてある罠を4つ独立に踏み直した**:

| 踏んだ罠 | 費やした時間 | スキルの該当箇所 |
|---|---|---|
| **新品端末の CalDAV 初回追加が「停止中」になる** | **約7分**(総時間の 38%) | 🔴 専用の節。回避策は `webcal://` 前置き(4タップ) |
| `idb ui text` の文字化け(`@` → `2`) | 数分 | 「テキスト入力」節。`simctl pbcopy` へ逃がす |
| スクショが `/tmp` に書けない | 1手 | 「その他のハマりどころ」 |
| AX ツリーにリンクが出ず詰まる | 約2分 | `diagnosis.md` §3「describe-all の穴」 |

**回避策の質も違う。** baseline は「削除 → 再追加」の儀式(約30タップ・入力6回)に辿り着いたが、
with_skill 側は `simctl clone`(**16秒・0タップ**)を選んだ。同じ問題に対する解の質の差が、
そのまま時間差になっている。

## 推移(eval-demo-video)

| iteration | スコア | 所要 | 備考 |
|---|---|---|---|
| 1 | 2/6 | 3302s(55分) | 採点を後日訂正(自己申告を検証せず PASS にしていた) |
| 2 | 2/6 | 1018s(17.6分) | 成果物ゼロ。**共有スキルを一度も読んでいない**ので測定として不成立 |
| **3** | **6/6** | **852s(14分12秒)** | 初完走。実尺 33.9s・実フレーム 1017・変化区間 97% |

## 未取得

- `without_skill` は **eval-caldav-account-sync の1本だけ**。demo-video 側は未取得。
- 残り4本の eval(状態プロビジョニング / WebView 逃がし / 日本語入力 / onboarding)は未実行。
- **`ios-skills:simulator-operator` 経由の実行が未検証。** 上記はすべて `claude` 型で回して
  おり、実運用の委譲経路(agent が `skills` で事前ロード)とは異なる。

## この表を更新するときの注意

**基準を後から緩めて点を上げない。** assertion を改訂したら、過去の採点は当時の文面のまま
確定させ、改訂は次の iteration から適用する(`evals.json` の `_assertion_revisions` に
before/after と理由を残す)。
