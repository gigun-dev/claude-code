# trigger A/B の事前登録(2026-08-02・結果を見る前に書いた)

**測るもの**: description を 02:15 版 → 現行版 に変えたことで、**発火の的中が良くなったか**。

| | 版 | 文字数 |
|---|---|---|
| **old** | `c08feb6`(762→358行 再構成時。**25 run の測定中に実際に live だった版**) | 415 |
| **new** | `HEAD`(`8e8cdf5` 07:47 + `b9d6b8d` 08:20 の編集後) | 474 |

**物差しは動かさない**(METHODOLOGY §6)—— `trigger_queries.json` の 20 問は
description を変えたあとも**一字も書き換えていない**。`trigger_queries.README.md` 参照。

## 手続き

- ハーネス: skill-creator の `scripts/run_eval.py`(`claude -p` を subprocess で叩く。
  **API キーは不要**だった —— 以前「API キーが要る」と書いたのは誤り)。
- 20 問 × 3 run × 2 版 = 120 invocation。`--trigger-threshold 0.5`。
- **project root は scratchpad の空ディレクトリ**にする。理由: caldav リポジトリで走らせると
  SessionStart フックが 120 run すべてに `next-directions.md` を注入して汚染する。
  ⚠️ **代償**: 実運用の「他スキルとの競合」が再現されない。したがって**絶対水準は実運用と違いうる**。
  両版が同じ環境を通るので、**差分**の比較としては成立する。

## 閾値(到達可能性を先に検算した —— METHODOLOGY §2)

- 最良ケース: new 20/20 vs old 14/20 → 差 6。**差 ≥3 は到達可能。**
- **判定**: `new − old ≥ 3` → 改善と認める / `≤ −3` → 悪化 / `|差| ≤ 2` → **差なし**。

## 打ち切り条件(METHODOLOGY §3)

**この 1 回で終わり。** 閾値に届かなくても追加 run はしない。

## 事前予測(結果を見る前に書いた)

**総合予測: 差は出ない(`|差| ≤ 2`)。** 根拠は下の相殺。

| id | 予測 | 根拠 |
|---|---|---|
| **t06** onboarding | **new が悪化** | old は「onboarding を試して」を**明示していた**。new はこの語を削った |
| **t14** XCUITest(陰性) | **new が改善** | old は「反復検証は XCUITest への昇格」を含み、陰性クエリを**誤発火させる引き**になっていた。new は削除 |
| t05 端末量産 | 微改善 | new は「端末を複数用意して」という**発話形**を足した |
| t03 デモ動画 | 両方通る | 両版に「デモ動画を撮って」がある(JSON の note は**それ以前の版**を指した記述で古い) |
| t18 Simulator 起動不能(陰性) | **両方落ちる** | 両版とも「Simulator まわりでうまくいかないとき」で引く |
| t01,t02,t04,t07〜t10 | 両方通る | 両版が同等に覆っている |
| t11〜t13,t15〜t17,t19,t20 | 両方通る | 両版とも引きが無い |

**この予測が外れたら、外れたこと自体を記録する**(当てるための登録ではない)。
