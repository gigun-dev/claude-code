# harness の現在地・次の作業

**正典。** 整理で追記を本文に反映したら、この節に「第n版」と記す。

> **書式の規約は `.claude/rules/harness.md`**(`docs/harness/**` で発火)。冒頭に二重に置かない。
> **冒頭に置くのは3種類だけ: 次の着手 / 未検証の危険 / 裁定待ちの判断。**概観は書かない。
> **8,000字 以内かつ ≒3,000 tok 以内・足すときは同量を削る**(#70460 で **10,000 文字**超は
> 無言に切り詰められる。トークンは毎セッション払う実費)。**行は予算にしない** —— どの機構も
> 使っておらず日本語率で 2 倍以上ブレる。実測は `/harness:status` が出す。
> 根拠と概観は詳細部と `log.md` へ。理由は詳細部の「冒頭の設計根拠」。

## 現在地(2026-08-10・第4版)

**未検証の危険:**

- **配布先の pre-push が関門として死んでいる可能性(→ `H-27`)。** `--check-cmd` に複合コマンド
  (`a && b`)を渡した配布先では前半の失敗が素通りする。**配布元は単一コマンドなので影響なし**を
  確認済みだが、**どの配布先が複合で入れたかは未調査。**
- **CI(verify.yml)の「落ちる側」を GitHub 上でまだ見ていない**(green 3回のみ。rc=1 はローカル実測)。
- **トークン概算は非ASCII で ±15% ブレる。**予算の判定はその幅があるものとして読むこと。
  ⚠️ **警告が消える方向の較正値は採らない。**根拠は詳細部「冒頭の設計根拠」。
- **配布先が旧単位のまま**(2026-08-08 に冒頭の予算をバイト/行 → 文字/トークンへ変更)。
  旧版の行ベース警告を出し続けるので `/harness:doctor` の再実行が要る(→ `H-6`)。

**裁定待ち:**

- **`H-24` を採るか。** 線引き案は項目本文(doctor=構造 / status=内容。両方に置かない)。
- **`H-22` の直し方。** ユーザー指示により**着手保留**。軸は項目本文。
- **`compact` matcher を残すか。** 実測で発火(7回)。ただし**冒頭は「開始時の方向づけ」なので
  compaction 後に消えてよい**(正典のポインタは CLAUDE.md 側で生き残る)。`fork` は未確認(→ `H-7`)。
- **完了主張のゲート(nylas 型 `PreToolUse [Bash]`)と `@yawlabs/ctxlint` を足すか。**
  論点は「増やした検知器が黙って死なないか」。詳細は `log.md` 2026-08-06。
- **Agent Plugins へ寄せるか。** skills 層は準拠済みなので急がない。
  **再訪条件: Claude Code が対応を表明 / `.claude-plugin/` が deprecated。**

**導入状況:** 詳細部。冒頭サイズと未移行は `/harness:status --all` が出す(手で維持しない)。

## 着手順(次にやること)

- [ ] `H-2` **転換3: 検知器を検証する**
      — install.sh の最後に検知器自身の動作確認を回す。
      ND の見出しから日付が抽出できるか / `comments.md` の glob が1件以上マッチするか。
      公式の `InstructionsLoaded` フック(`load_reason: path_glob_match`)なら実セッションでの
      発火まで確認できる。**swift-mcp-app の事故はこれで二度と起きなくなる。**
      → 完了条件: install.sh の実行末尾で「ND の見出しから日付が抽出できた」
        「`comments.md` の glob が1件以上マッチした」が実物に対して検証され、出力に出ること
- [ ] `H-7` **SessionStart の matcher に `resume` を足す**(`fork` は有効性が未確認)
      — `install.sh:111` が書くのは `startup|clear|compact`。**公式で確認できたのは
      `startup|resume|clear` の3つ**、`compact` は実測で発火(7回)。`--resume` / `--continue`
      では冒頭注入が一度も走っていない。`assets/codex-hooks.json` には `resume` が入っており、
      設計判断ではなく取りこぼし。`paths:` 不一致と同種のサイレント無効化。
      → 完了条件: `--resume` で起動したセッションで冒頭が注入されることを目視で確認する
  > **2026-08-10 更新:** 2026-08-10: install.sh v0.15.0 で matcher に resume を追加し、既存導入先の matcher も更新する冪等ロジックへ修正(6673d16)。使い捨てリポジトリで追加/更新/不変の3状態を実測済み。残るは完了条件の「--resume 起動で冒頭注入の目視確認」のみ
- [ ] `H-4` **転換1: フックを二層化する**(全面移行はしない。**2026-08-06 に結論が変わった**)
      — プラグイン常駐フックは発火する(実測)が、チーム・CI に届かず・無言の回帰があり・
      二重実行する。**プラグインには検出と警告だけ**(未導入 / hooksPath 未設定 / 世代が古い)、
      本体はコピーのまま。根拠と issue 番号は `log.md` の 2026-08-06。
      → 完了条件: 未導入リポジトリでプラグイン側が警告し、導入済みでは何も言わないこと
- [ ] `H-5` **転換2: 既存 ND の書式を移行**(冒頭から概観を落とし、完了を証拠付きに)
      → 完了条件: `/harness:status --all` で全リポジトリの冒頭が **8,000字未満かつ ≒3,000 tok 未満**・未移行0件
  > **2026-08-08 更新:** 2026-08-08 実測: 係数を 4.7 系トークナイザへ直した結果、harness の冒頭 ≒3,523 tok・ios-skills ≒3,012 tok で**どちらも予算 3,000 tok 超**。旧係数(旧トークナイザ)が超過を隠していた。文字数は 8,000 に対し余裕あり(4,322 / 3,823)なので、削るべきはトークン側 = 日本語の散文。
  > **2026-08-09 更新:** 2026-08-08: pointer 方式のリポジトリでは、この完了条件の「≒3,000 tok 未満」を裏付ける機構が無い(冒頭が SessionStart の stdout を通らないため切り詰めも毎セッションの実費も発生しない)。整理で冒頭を削ったあと、pointer 型の予算は現況へ下げ直したうえで単位を行へ切り替える(R-28。先に切り替えると警告を消すだけになる)
- [ ] `H-6` **未導入リポジトリへの展開と原型の取り込み**(詳細は詳細部「導入状況」)
      → 完了条件: 4リポジトリで刻印が返り、刻印なしファイルが残っていないこと
- [ ] `H-11` **skill の eval(発火の精度だけ)** — `jq` で tool_use を見るので LLM 審判が要らず、
      人が手で1回叩けば検算できる(= HITL が形骸化しない)。3クエリ×3回から。**出力品質の
      LLM 採点はやらない。**→ 完了条件: 「次何やるんだっけ」で status が出て tidy が出ないこと
- [ ] `H-9` **`.claude/harness-manifest.json` を導入する**
      — 世代刻印が測定対象を取り違えている(**上書きクラスは grep しても差が出ず、スキップ
      クラスは差が出るが更新できない**)。version + commit SHA + installed 一覧 + install 時の
      オプションを1枚に持つ。**引数が永続化され**、廃止ファイルの削除もできる。詳細は `log.md`。
      → 完了条件: 再実行時に前回のオプションを引数なしで再現できること
- [ ] `H-13` **doctor を Plan-validate-execute にする** — 未導入なら Vision(現状/導入後/変更点/**辞め方**)、導入済みなら差分。既定を非破壊にすると分離の理由が消える
      → 完了条件: 未導入 repo で Vision が出て承認後にのみ書き込むこと。辞め方の1行が必ず出ること
- [ ] `H-14` **hooks を3段構えにする** — 既存の仕組み(lefthook/husky/pre-commit/既存 hooksPath)を検出してその形式の断片を提案 / 空きなら同意を取って取る / **「入れない」選択を記録できるようにする**
      → 完了条件: lefthook を持つ repo で .githooks を持ち込まず断片を提案すること。pre-push 不採用を記録すると doctor が黙ること
- [ ] `H-15` **着手順に依存辺を足す**(`→ 依存: H-3`)。ID は名前であって順位ではないと決めた以上、順序を運ぶものが別に要る。ready/blocked を計算し status で出す
      → 完了条件: blocked が依存の完了で自動的に解け(アーカイブ済み ID を完了と判定)、循環を fail-closed で検知すること
- [ ] `H-22` [bug] check.sh が HTML コメント内の文字列を「禁止指示/自動化指示」として誤検知する — CLAUDE.md の <!-- --> コメント内が検査対象に入っている。実害は2つ: (1) 「その案は却下済み・再提案しないこと」という**却下の記録**を拾い、doctor がまさに却下された案を再提案する形になる — 却下理由をコメントで残すほど指摘が増える逆インセンティブ。(2) 「『…を必ず実行』は却下済み」のような引用部分を自動化指示として拾う。あわせて、記述文(「〜してはいけない理由がそこにある」= 参照先の案内)を禁止指示として拾う件も残っており、2026-08-05 に直した『可能表現の誤検知』と同型。期待: (a) <!-- --> の中は検査対象から外す (b) 『〜は却下済み』のような打ち消し文脈を拾わない 
      → 完了条件: 配布元で再現し、直したうえで、配布先(cf-asc-dashbord)へ再配布して解消を確認する
  > **2026-08-09 更新:** 2026-08-09 / v0.15.0 でも再現。ある配布先での実測: 「禁止指示」4件中3件が誤検知(2件は HTML コメント内・1件は『〜してはいけない理由がそこにある』という参照先の案内=記述文)、実在は1件。**誤検知率 3/4。** さらに『必ず〜する』1件も却下記録の引用部分だった。裁定の材料として: 今日その配布先で指摘が減ったのは検査が直ったからではなく**却下記録のコメントを別ファイルへ移したから**で、つまり現状は『理由をコメントで残すほど指摘が増える』が実際に起きている
  > **2026-08-09 更新:** issue #1(2026-08-05)の実例で v0.15.0 を実測: 「毎回ソースビルドに**なる**」(結果の記述)を自動化指示として拾う一方、同ファイルの「必ず git add す**ること**」(本物の指示)は挙がらない。**誤検知と見逃しが同時に起きている** —— 見逃し側は H-22 の本文に無かった観点。#1 は本項目へ一本化する
- [ ] `H-24` 鮮度検査を doctor から status へ移す(status が古い正典を古いと言わずに一覧表示している)
      → 完了条件: /harness:status が正典の日付より新しいコミットを検出して警告し、doctor 側からは鮮度検査が消えていること(両方に置かない)。⚠️ 着手前にユーザーの裁定が要る
  > **2026-08-09 更新:** 2026-08-09 の doctor 実行で誤検知を実測: harness だけを触った日(実際の変更は `.claude/rules` と `docs/harness` のみ)に、**触っていない ios-skills の正典まで「古い」と指摘された**。原因は `check.sh:505` の `lc=$(git log -1 --format=%cs)` —— パス制限が無いのでリポジトリ全体の最新コミットと比べている。**tidy 側は 2026-08-06 に同型を修正済み**(log.md 参照)なので、doctor 側だけが取り残されている。移設するにせよ残すにせよ、**コンポーネント別判定は必須**
- [ ] `H-25` [bug] doctor SKILL.md の起票手順が report.sh v0.15.0 と乖離している(--create / --github は存在しない)
      → 完了条件: SKILL.md の「When the check itself is wrong」節が inbox 方式を説明しており、--create / --github / 2表の可視性分割の記述が残っていないこと。あわせて『inbox に書いただけでは上流に起票されていない』ことと、上流へ載せる手順(nd-tasks.sh --add をパス明示で呼ぶ)が書かれていること
- [ ] `H-26` [bug] 「却下は書き残せ」と「CLAUDE.md を太らせるな」が衝突する — 記録先の推奨順が逆
      → 完了条件: SKILL.md の失敗モード1が、却下記録の第一候補を**注入されない領域**(正典の詳細部 = session-head-end より下)にしていること。CLAUDE.md は『規則のすぐ隣に理由が要るとき』の例外に格下げし、その場合もポインタ1行に留めるよう書かれていること
  > **2026-08-09 更新:** 実測: 指示どおり CLAUDE.md へ却下理由を書いた配布先で 2,828→3,144 tok に増え、**増分がまるごと却下の記録**だった(次の実行で『大きすぎる』を再指摘される = 従うほど怒られる)。却下記録の役目は『次のセッションが同じ議論を蒸し返さない』ことで**毎セッション注入される必要は無い**。全文を詳細部へ移しポインタ1行だけ残したら 3,144→2,499 tok になり、記録は1文字も失っていない。H-22(コメント内の誤検知)と根が同じ —— どちらも『理由を書き残すほど損をする』形になっている
- [ ] `H-29` SessionStart 配線検知 — session-start.sh に core.hooksPath の指す先が現作業ツリーに実在するかの検査を足し、未配線なら警告1行(自己修復はしない)。依存: H-7(matcher 穴の相続を防ぐ)
      → 完了条件: hooksPath 未設定 / .githooks 未チェックアウトの worktree の両方で警告が出て、健全時は無出力であることを実測すること
- [ ] `H-31` 配布元に CLAUDE.md(実体)+AGENTS.md(symlink)を導入し、作業リズム(1コミット=1論理変更 / 短命ブランチ=1着手順項目 / 依存を跨がせない / 並行は worktree / push 前に tidy)と gitops 語彙を置く。却下記録の実体は入れない(H-26)。CLAUDE.md 不在却下の再検討条件「横断の常時ルールが実際に要るまで」が 2026-08-10 に成立した再裁定
      → 完了条件: CLAUDE.md が ≒2,000 tok 以内で、doctor の「CLAUDE.md が無い」指摘が消え、AGENTS.md 経由で同内容が読めることを確認すること
- [ ] `H-32` 着手順ボードの生成器を status skill に恒久化する。テンプレ+生成スクリプト1本(--format html 相当)を正典に置き、出口を3つ共有: (a) post-commit hook が gitignored なローカル HTML を再生成(トークンゼロ、PC 用) (b) tidy の締めで Artifact 再公開(スマホ用、file_path 渡しなので中身はコンテキストを通らない) (c) status/tidy からの明示呼び出し。行には git クエリ由来のブランチバッジ(git log --all --grep <ID>。対応表は持たない — R-却下済みの方針どおり保存でなく到達性)、現在地の折りたたみ埋め込み、カタログ節目次も足す。エージェントに毎回 HTML を書かせない(原則3)。依存: H-28(post-commit は同じ hook 配置パターンに乗る)
      → 完了条件: post-commit 後にローカル HTML が自動更新されること、ボードにブランチバッジと現在地が出ること、生成にモデルが関与しないこと(スクリプト単独で完結)を実測すること
  > **2026-08-10 更新:** focus ビュー(依存辺の近傍描画。依存: H-15)と MCP Apps+Tasks の第4出口候補を設計に追加。全文は log.md 2026-08-10「H-32 の設計メモ」— 冒頭の予算超過(10,006字)を検知したため詳細をそちらへ降ろした
  > **2026-08-10 更新:** 2026-08-10: 生成器 render-board.sh v0.1.0 が 8d06859 で main へ(worktree 並行の初実践)。項目本文の git log --all --grep --format='%D' は実測で両方向に壊れており(無関係ブランチ誤付着+2コミット手前で無検出)、実装は main..<branch> 未マージ差分方式に訂正済み。残: post-commit 配線と status SKILL.md の到達性(fable 3.5 と同時)

<!-- session-head-end: ここから下は詳細部。着手する節をそのとき読む -->

## 完了記録(着手順から降ろしたもの)

冒頭は予算制なので、完了した項目はここへ降ろす。**ID は再利用しない**(log.md から参照されるため)。

- ~~`H-1` `install.sh` の `core.hooksPath` 無条件上書き~~ ✅ 2026-08-07(`install.sh v0.12.0`)
  — 既存値を読んで3分岐(未設定=従来どおり配線 / `.githooks`=何もしない / **別の値=絶対に触らない**)。
  **→ 使い捨てリポジトリ3件で実測。**`core.hooksPath=.git-hooks` のケースで値が保たれ、⚠️ TODO が出て、
  既存のカスタムフックが実際に発火することまで確認(`existing custom pre-push ran`)。
  読む側(doctor が `.githooks` を起点に探すため別 hooksPath を「無い」と誤検知する)は未修正 → `H-10`。
- ~~`H-8` `docs/ios-skills/next-directions.md` を整理する~~ ✅ 2026-08-07
  — 着手順まで冒頭に入れると 11KB になり #70460 の 10KB 無言切り詰めを超えていた。詳細を
  マーカー下の「着手順の詳細」へ ID 別に移した。**→ 7,067B・`session-head-end` あり を実測。**
  ただし **88行**で、後から足した 80行 予算には収まっていない(→ `H-5` で回収)。
- ~~`H-10` **doctor の pre-push 検査の起点を `core.hooksPath` の実値に変える**~~ ✅ 2026-08-07
  — 今は `.githooks/pre-push` の存在を起点にし、無ければ else で「pre-push が無い」と言う
  (`check.sh:138`)。dotfiles は `core.hooksPath=git/hooks` に実在するのに誤検知する。
  hooksPath を先に読み、未設定なら `.git/hooks/` も見る。harness 製かは刻印で判定。
  → 完了条件: dotfiles で誤検知が消え、claude-code では「無い」が残ること
  → 2026-08-07 / dotfiles(core.hooksPath=git/hooks)で『pre-push が無い』が『✓ 配線済み(harness-template。hooksPath: git/hooks)』に変わり、claude-code では『無い』が残ることを check.sh v0.8.0 で実測
- ~~`H-3` **不具合1・2を直す**~~ ✅ 2026-08-08
  — 鮮度検査の正規表現(日付直後の閉じ括弧を要求している)、
  survey.sh の世代検出が rules の説明文を拾う。
  → 完了条件: `## 現在地(2026-08-05・第1版)` 形式の実物で鮮度検査が発火すること、
    survey.sh の世代検出が `harness-template v (未導入)` を出さないことを確認する
  → 2026-08-08 / (1) 鮮度検査: 見出し行を特定してから日付を拾う形に変え(session-start.sh v0.3.0 / check.sh v0.10.0)、使い捨て repo で4ケース実測 — 実物の `## 現在地(2026-08-06・第2版)` で発火・見出しに日付なしで警告・見出しごと無しで警告・全角括弧+日付が先頭でない形でも正しく読めることを確認。(2) survey.sh v0.9.0: 正規表現の `[0-9.]*` が0文字マッチして rules の散文を拾う件と、`|| echo` がパイプラインの終了コードのせいで永久に発火しない件の2つを修正 — 配布元で「(未導入)」、dotfiles で「v0.2.0 v0.2.1」が出ることを実測
- ~~`H-12` **3本化する**(`init`→`doctor` 統合、`next`→`status` 改名)。設定配線は doctor 1本、正典は status/tidy。名前は借りたモデル(git status / brew doctor)から取り、期待も一緒に借りる~~ ✅ 2026-08-08
  → 完了条件: 3本になり、init が他を名指しし(現在0件)、旧 /harness:doctor への参照が repo 内から消えていること
  → 2026-08-08 / skills/ を 4本→3本にした(next→status へ git mv、init を doctor へ統合し install.sh / survey.sh / assets/ を移設、導入手順は doctor/references/installing.md へ progressive disclosure)。SKILL.md 3本を英語で書き直し(doctor ≒2,219 / status ≒2,647 / tidy ≒1,939 tok、5,000 上限に対し余裕)、相互案内も3本すべてに。repo 内の /harness:init /harness:next 参照は log.md(時点記録なので保存)を除き 0 件を grep で確認。plugin.json を v0.15.0 と3本構成の description に更新。
- ~~`H-18` 書式の散文をポインタ1行へ寄せる(配布テンプレの HTML コメント / .claude/rules/harness.md)~~ ✅ 2026-08-08
  → 完了条件: 着手順の書式が nd-tasks.sh の --help と --lint 以外に散文で書かれていないこと。配布テンプレと rules は「書式は /harness:status --help が正」の1行ポインタになっていること
  → 配布テンプレ v0.14.0 と .claude/rules/harness.md から書式の再掲を削除し nd-tasks.sh --help へのポインタ1行に置換(cf851cc)。--help に無い情報は残した。--lint / check.sh / verify.sh すべて rc=0
- ~~`H-19` editor.awk がパーサの出力で位置を特定するようにする(書式の二重実装を1本に)~~ ✅ 2026-08-08
  → 完了条件: 書式の解釈が parser.awk 1箇所になり、editor.awk が独自の項目照合を持たないこと。先に --lint を回帰テストとして固めてから着手する
  → editor が自前の節探索・ID 照合を持たなくなり、parser の位置情報を -v layoutfile で受ける形にした(28af681, v0.16.0)。--add/--done/--note/--archive の書き込み結果が旧版とバイト一致、境界4ケースで md5・exit code も一致、差は意図した1点のみ
- ~~`H-20` report.sh を ~/.harness/inbox/ 方式へ単純化する(可視性ゲート・欠陥/証拠の分割・GitHub 起票を撤去)~~ ✅ 2026-08-08
  → 完了条件: git の外にローカルのメモを書くだけになり、/harness:status が未処理の件数を出すこと。still 184行ある機構が大幅に減っていること
  → report.sh v0.6.0 で 184行→112行、gh 依存ゼロ、~/.harness/inbox/ へ1件1ファイル(f6f0b47)。インジェクション試行が文字列として書かれ実行されないこと・bash 3.2 で空配列 rc=0 を実測
- ~~`H-21` 敵対的検証(2026-08-08・3体)の残り指摘を潰す — 詳細は log.md 同日~~ ✅ 2026-08-08
  → 完了条件: log.md の一覧のうち深刻度 高/中 がすべて修正または却下(理由つき)になっていること
  → 残10件を全処理: 修正7件(節境界=H-19 / log-index のフェンス b1ac338 / SIGPIPE ddb7e37 / plugin.json 版数 bc1fae4 / 完走マーカーの読み手 7244ee5 / principles 規則7 の模範例 e60a77a / 並列 --add の lost update bb0399a)、却下3件(--archive の詳細部への書き込み=2形状で再現せず / disable-model-invocation=バイナリに17ヒットで実在・外すと Claude Code でも自動発火し悪化 / 日本語固定=英語正典のリポジトリが0件、再検討の引き金は英語正典への導入時)。追加で check.sh の paths 引用符なし誤報と /tmp 書き込みも 269f6ab で修正
- ~~`H-17` トークン概算の係数を /context で較正する~~ ✅ 2026-08-08
      → 完了条件: 英語 0.25 / 日本語 1.1 tok/字 を /context の実測と突き合わせ、context-mechanics.md §4.5 の未較正 ⚠️ を消すこと
      → transcript の usage(Anthropic 側の実トークナイズ結果)669観測から最小二乗で較正し、大きい6本を calibrate.sh --manual へ通した(0b751f4)。ASCII は全標本 0.345 / calibrate 0.37 でどちらも既定 0.33 超 = 過小評価だったので 0.35 へ。非ASCII は 125 が出たが据え置き —— 日本語率 40〜47% でも 1.18〜1.60 に散らばり分解できておらず、採ると ios-skills の警告が文書を変えずに消えるため。context-mechanics.md の未較正 ⚠️ を実測結果へ置換
  > **2026-08-08 更新:** 2026-08-08: calibrate.sh を追加(doctor 配下・明示起動専用。--gpt はローカル tiktoken、--claude は count_tokens API で無料)。**GPT/o200k は実測済み: ASCII 25 / 非ASCII 102、日本語率 1%/25%/44% の3本で最大残差 0.8%** —— 線形モデルは成立する。**残るのは Claude 側**(API キーが無く未測定。既定 33/140 は文献値)。
  > **2026-08-08 更新:** 2026-08-08: ANTHROPIC_API_KEY 未設定・/context はエージェントから実行できないため、この項目はユーザーの手が要る。/context の出力を calibrate.sh --manual へ渡せば即座に閉じられる。GPT 側(o200k)は測定済み
- ~~`H-27` [bug] pre-push が複合 check-cmd で素通りする(関門が機能しない)。`assets/pre-push:46` が `if ! {{CHECK_COMMAND}}; then` のため `a && b` が `(! a) && b` になる。**詳細・再現・修正案は issue #2**(2026-08-05 起票、v0.15.0 の現物でも未修正を実測)~~ ✅ 2026-08-10
  → 完了条件: assets/pre-push が {{CHECK_COMMAND}} をグループ化しており、前半が失敗する複合コマンド(例 `--check-cmd 'false && true'`)を入れた使い捨てリポジトリで push が実際に中止されることを実測すること
  → 2026-08-10 / 34cde17 / 使い捨てリポジトリでバグ再現(true && false で push 通過)→修正後に中止を実測。注意: 正典の例示 'false && true' は判別力ゼロ(バグ版でも偶然中止する)と判明し 'false && false' へ訂正
- ~~`H-28` pre-commit を素の sh で新設(非ブロッキング)。CLAUDECODE/AI_AGENT で agent 検出、staged files を .claude/rules/*.md の paths glob と突き合わせて読み直しを促す。スコープ横断(2 rule 以上)で粒度警告。正典未更新のコード commit 検知で tidy を促す(2026-08-10 に真陽性3件の実測あり)~~ ✅ 2026-08-10
  → 完了条件: 使い捨てリポジトリで、rules スコープに触れる staged で注意が出て、agent 環境変数なしでは無音で、内部エラー時も exit 0 であることを実測すること
  → 2026-08-10 / ff34b7e / 使い捨てリポジトリで9シナリオ(コンポーネント別判定・brace fail-open・パーサ破損警告・非agent無音を含む)+実リポジトリで検査Aと head-costly の実発火を確認。実行時間 90–140ms
- ~~`H-30` pre-push ゲート自己テスト — doctor が HARNESS_PREPUSH_SELFTEST 経由で must-fail(false && true)と must-pass を注入して関門の生死を検査。依存: H-27(修正の回帰テストを兼ねる)~~ ✅ 2026-08-10
  → 完了条件: H-27 修正後の pre-push に対し must-fail が exit 1 / must-pass が exit 0 になることを doctor 実行で実測すること
  → 2026-08-10 / 34cde17 / must-fail(false && false)が rc=1・must-pass(true)が rc=0 を配布元実弾で確認。H-27 を意図的に再導入した pre-push で must-fail が素通りを検出することも実測(検知器の検証)
- ~~`H-33` agy_search の出典を --json-schema による構造強制に変える(いまはプロンプトで「## 出典を付けよ」と依頼しているだけで、頼まないと付かない回がある)~~ ✅ 2026-08-10
  → 完了条件: sources を minItems:1 のスキーマで要求し、出典ゼロを fail-closed で弾けること。返り値は読めるテキストのまま、生成元だけをスキーマ強制の JSON にする
  → 却下(実装せず)。現行の散文プロンプトで4クエリ試して4/4で「## 出典」節と URL が付いた。--json-schema 版はむしろ出典が1件に減った。詳細と却下理由は log.md R-47

## 盆栽の7原則(2026-08-07 確定)

**ハーネスに何かを足す/変える前に、これで採点する。**圧縮版が
`.claude/rules/harness.md` にあり、`plugins/harness/**` と `docs/harness/**` を触ると自動で届く
(原則7を、この原則自身の配送に適用している)。

すべて**この repo で実際に壊れた事例**から出ている。順序は独立で、上位ほど効くわけではない。

### 1. 置き場は「正しさを維持する義務があるか」で決める

| | 維持義務 | 腐ったときの症状 |
|---|---|---|
| `log.md` | **無い**(その時点の記録) | 古くなるだけ。害が無い |
| コメント | 弱い(コードと一緒に動く) | コードと乖離すると嘘になる |
| `next-directions.md` | **ある**(現在地を表明する) | 嘘になる |
| ADR 相当 | **ある** | 嘘になる。しかも誰も気づかない |

Write the Docs の「**誤ったドキュメントは無いより悪い**」が適用されるのは維持義務がある側だけ。
log.md が安いのはここ(Simon Willison の temporal documentation —— 通常のドキュメントは
「将来の更新義務」を伴うが、時点記録は伴わない)。

**使い方**: 書きたくなったら「これは維持しないと嘘になるか」を先に聞く。嘘にならないなら
log.md、なるなら**維持コストを払う覚悟があるか**を確認してから置く。
→ ADR を入れなかった判断はこの一点(実測: ADR ログの 78.7% が1年以上放置、
24.6% が30日以内に停止。MADR 本家ですら 19本中3本しか `status:` を運用していない)。

### 2. 事実と機構は残り、散文は腐る

AHE(arXiv:2604.25850)の ablation —— ハーネスを7コンポーネントに分け、種に1つだけ差した:

| 差したもの | Terminal-Bench 2 |
|---|---|
| memory のみ | 75.3% |
| tool のみ | 73.0% |
| middleware のみ | 71.9% |
| **system prompt(= 散文)のみ** | **67.4%(唯一の退行)** |
| 種(何も差さない) | 69.7% |

> factual harness structure transfers across tasks and models whereas **prose-level strategy does not**

Claude Code 作者も「半年ごとに CLAUDE.md も skills も hooks も全部消して、モデルが何をするか
見ろ」と言っている。ETH の A/B(arXiv:2602.11988)の「指示は従われるが概観は効かない」も同型。

**使い方**: 書くのは罠(`idb ui tap` は外しても exit 0)・実測値(753s vs 852s)・機構
(ゲート、検知器、スクリプト)。**「まず〜を検討せよ」「慎重に判断すること」は書かない。**

### 3. 予算を先に置くと、規律が要らなくなる

冒頭を 8KB に縛った結果、「根拠は log、冒頭は裁定待ちだけ」という書き分けが**自動的に強制された**。
実際 2026-08-07 に項目を足したとき 9,189B になって検知器に捕まり、削らざるを得なかった。

**制約が判断を代行する。**判断に任せると必ず膨らむ。

⚠️ **閾値を上げて警告を消すのは禁止。下げ直すのは可。**上げ方向を許すと検知がラチェット式に
静かに死ぬ。現行の予算: 冒頭 **8,000字**(#70460 / #84021 で 10,000字 超は無言に切り詰め)かつ
**≒3,000 tok**(毎セッションの実費)/ CLAUDE.md **≒2,000 tok** / log 1,500行
(全文ロードの費用から。実測で caldav が 1,424行 / 146KB とすぐ手前)。

> **2026-08-08 更新: 単位を「バイト/行」から「文字/トークン」へ変えた。**旧予算はどちらも
> **どの機構も使っていない単位**だった —— #70460 の "10K" を私がバイトと読み違えており
> (原文は 10,000 **文字**)、行は言語構成で 2 倍以上ブレる(`figmate/CLAUDE.md` は 88 行で
> 警告対象だったが実費は ≒1,743 tok で健全 = 行で測っていたことが生んだ誤検知)。
> **代理指標は人が目視で数えるときに要るもので、スクリプトが測るなら実単位で出せる**
> —— 原則「決定論的な作業はスクリプトへ」と矛盾していた。判断規則は `docs/principles.md`
> 規則8。⚠️ **実効は緩和側だった**(冒頭は旧 97% → 新 51%)。緩和だという事実は隠さない。
> log の 1,500行 が行のままなのは意図的 —— **log は注入されない**ので守っているのは
> 「人が読み通せる長さ」で、それは実際に行で決まる。**単位は機構ごとに選ぶ。**

### 4. 検知器は必ず黙って死ぬ。だから検知器を検証する

このリポジトリで実際に起きた空振り:

- 鮮度検査の正規表現が2リポジトリで一度も発火していなかった(全角括弧の件を直した後の**再発**)
- `survey.sh` の世代検出が、自分の説明文を拾って `harness-template v (未導入)` を出していた
- **`docs/harness/log.md` の見出しが、数時間前に書いた自分の規約に違反していた**
- 冒頭のバイト数を測るとき、本文に `session-head-end` と書いたせいで grep が誤爆した
  (実装側は行頭アンカーでこれを防いでいた)

**共通の症状は「エラーが出ない」こと。**だから **0件は「無い」ではなく「壊れた」として扱う**
(`nd-tasks.sh` は「タスクが無いと解釈してはいけない」と出して落ちる)。
そして **規約を書いた本人が守れているかを機械が見る。**

逆向きの故障もある —— **黙る代わりに、それらしい数字を出し続ける。**
Langfuse 連携は「ツール失敗 ERROR 489件」を報告し続けたが、本物の失敗は1件も記録されて
いなかった(`PostToolUseFailure` 未購読。489件は成功したツールの出力に `"rror"` が含まれる
だけの誤検知)。同期間の実失敗は cclens 実測で Edit 5件 —— **64倍の嘘**。独立した第二の計測と
同じ期間で突き合わせたときだけ割れた。**「0件を疑え」では捕まらない —— 数字が出ているから。**

だから検証は2段:**発火したか**(0件検査・実物に対するテスト = `H-2`)と、**中身が正しいか**
(独立した第二の計測との突き合わせ。`calibrate.sh` が概算トークン係数を実トークナイザで較正
するのがこの形)。**突き合わせを一度も通っていない数字は「未検証」として扱い、断定の根拠に
しない。** 経緯は `log.md` 2026-08-08。突き合わせ自体のスクリプト化は見送った(`R-29`)。

### 5. 強制は最小、検知は最大

「a rule is only hard if a hook blocks it」は正しい。だがゲートを増やすと逆効果になる、という
一次証言が正面から対立している:

> ゲートが無いから死ぬ —— "no gate → writing one is volunteer work. That's why they die."
> **ゲートがあるから死ぬ** —— "if the document has to be approved, then people
> **quietly make decisions without recording it anywhere**."

changesets 公式も「changeset が無いことでブロックすることは**推奨しない**」と明言し、
`--empty` の逃げ道を用意している。

**使い方**: 落とすのは **CI 相当の検証(pre-push の型・テスト)だけ**。記録・文書系は
**警告 + 明示的な逃げ道**(`- (記録不要: 定型作業のみ)` がそれ)。

### 6. 新機構を足す前に、既存機構で「届くか」を見る

ADR の件が典型だった。診断はこう:

- 却下記録は**既に書けていた**(copier 見送りの理由は PEP 3103 の却下通知より詳しい)
- 置き場も**既にあった**(log.md)
- **無かったのは ID と索引だけ**

**問題は保存ではなく、編集地点での到達性だった。**新しいディレクトリを作っても到達性は
改善しない。

### 7. 距離が近いほど効く。ただし距離を詰めるために複製すれば必ずドリフトする

> **2026-08-10 追記: 「正典は1箇所」の「1箇所」にはブランチも含む。権威はデフォルトブランチ
> (main)の版にあり、他ブランチ/worktree の ND は作業中の草稿 —— マージで権威に合流する。
> feature 側では冒頭(現在地・裁定待ち)を書き換えず、着手順の自分が着手した項目だけ触る。**
> 根拠と経緯は log.md 2026-08-10「gitops と skill 分類の裁定」。

Tailwind が勝ったのは「このクラスは何をするのか / どこで定義されているか」を調べる**往復が
消えた**から。コンポーネントを1つ読めば全部わかる。エージェントには**2つ目のファイルを
開かせない**ことが効く。

同じ構造が繰り返し出ている:

> **ADRs, Notion docs, and Confluence pages die because they're separate from the code.
> Out of sight, out of mind.**

Write the Docs の原則にも1項目ある —— "**Nearby**: Store sources as close as possible to the
code which they document."。ETH の「指示は効くが概観は効かない」も距離で説明がつく:
**編集地点に届く指示は行動に接続されるが、セッション開始時に届く概観は何にも接続されていない。**

**ただし代償がある。**Emil Kowalski のスキル群は自己完結のため値を7ファイルに複製し、結果
frequency 表が3文言に、scale 下限が3種に分裂した。Tailwind がこれを回避しているのは
**値(design token)を1箇所に集め、表現(utility class)だけをその場に置いている**から。

**使い方**: その場に置くのは**事実とポインタ**。正典は1箇所。
`.claude/rules/*.md` が「テンプレートの先頭が正(複製しない)」と書いているのがこの形。

### この7原則で採点した結果(2026-08-07)

| 提案 | 判定 | 効いた原則 |
|---|---|---|
| log.md の見出し移行 + 索引 | やる | 1(義務なし)・6(既存機構) |
| 却下に `R-n` の ID + 却下索引 | やる | 6(無いのは ID だけ) |
| 「裁定待ち → 却下は log へ」 | やる | 1(ND から義務が減る) |
| `paths` 付き rule に却下理由を3行 | やる | 7(編集地点に届く)・2(事実) |
| **ADR ディレクトリ新設** | **やらない** | 1(義務が増える)・6(到達性が改善しない)・7(遠い) |
| **pre-push で log 欠落を fail** | **警告どまり** | 5(隠れるようになる) |

却下案は**アンカーがあるならコードコメント**(curl の "We used to close the filters ... however
that complicates filter code" が実例)。アンカーが無いもの(「copier に乗り換えない」等)だけが
log.md + `R-n` に落ちる。

## 2026-08-10 の裁定(冒頭から降ろしたもの)

- **skill の分類**: pull(doctor/status = 人が引く)/ push(tidy = エージェントが押す)。
  status は3行サマリ+テーブル、tidy が出口で status を呼ぶ(status ⊂ tidy)。**実装は未着手。**
  却下側は `log.md` R-39/R-40。
- **`plugins/agy-mcp/` を新設**(検索・任意プロンプト・会話の追撃・媒体)。
  **agy は画像も動画も見られる** —— agy 側の `~/.gemini/antigravity-cli/settings.json` に
  `"permissions": {"allow": ["read_file", "read_file(*)"]}` が要る。YouTube は yt-dlp で
  落として渡す。**Gemini API キーは要らない。**⚠️ **この設定が無いと `status:"SUCCESS"` の
  まま空応答**になり、モデルが「見られない」と言っているように読める。実測と却下 R-41〜R-47 は
  `log.md` 2026-08-10(誤った結論を出した経緯ごと残してある)。
- **agy 経由で他ベンダーのモデルが使える**(実測: Claude Opus 4.6 / Sonnet 4.6 / GPT-OSS 120B)。
  Antigravity のサブスク枠なので追加コスト無し。セカンドオピニオンに使える。

## 冒頭の設計根拠

**なぜ2つの単位で測るか。** 文字は**切り詰め**の単位(SessionStart の stdout は 10,000字超で
無言に切られる・#70460)、トークンは**毎セッション払う実費**の単位。壊れ方が違うので
片方だけでは足りない。**行は予算にしない** —— どの機構も行を見ておらず、日本語率で 2 倍以上ブレる。

**トークン概算の較正(2026-08-08〜09 実測)。** ASCII は 0.35 に較正済み(transcript の `usage` =
Anthropic 側の実トークナイズ結果。API キー不要)。**非ASCII は 1.40 を採ったが分解できていない**
—— 日本語率 40〜47% の観測でも 1.18〜1.60 に散らばり、最大残差 9.5%(o200k は 0.8%)。
原因は1ターンの差分に**数えられない挿入が ≒1,300 tok** 混ざること。
⚠️ **較正値 1.25 を採ると ios-skills の警告が文書を変えずに消える**ので採らなかった
—— 警告を消す方向の較正は、較正ではなく閾値の引き上げである(原則3)。
確定には count_tokens API か、日本語のみの大きなファイルを単独ターンで読む測定が要る。

**`allowed-tools` を SKILL.md に付けていない理由。** あれは「許可」ではなく「制限」として
解釈されるため、付けるとスキルが本来使える道具を失って壊れる。

## 導入状況

**手で維持しない。** 冒頭のサイズ・未移行の有無は `/harness:status --all`、配布物の世代は
`/harness:doctor` が出す。ここに書くのは**機械が出せない質的な事情**だけ。

| リポジトリ | 機械が出せない事情 |
|---|---|
| store-redirect | 初適用の実験台。最新相当 |
| caldav | comments.md / pre-push がテンプレ以前の原型。**原型の方が良い箇所があれば還流させる** |
| swift-mcp-app | 全部原型(刻印なし)。`interaction.md` が `paths` 無しで常時ロードされている |
| claude-code(この repo) | pointer 型フック(マーケットプレイス向けの独自版)。テンプレート版とは別物 |
| tdr-concierge | **未導入**(`.claude/hooks` 自体が無い)。2026-08-06 に「注入が停止中」と誤報告した |
| tiktok-fetcher / cf-asc-dashbord | check スクリプトは用意済み・ハーネス未導入 |

## 方針の外部検証(2026-08-05 調査)

**結論: 乗り換え先は無い。harness の思想は業界の現在地と一致している。**
変えるべきは思想ではなく (1) 配り方 (2) 冒頭に注入する中身の性質 (3) 検知器そのものの検証。
**価値の本体は規約側にあり、配布物は薄いほど良い。**

- **ETH Zurich [arXiv:2602.11988](https://arxiv.org/abs/2602.11988)** — SWE-bench Lite +
  実物の AGENTS.md を持つリポジトリでの A/B。「コンテキストファイルは成功率を改善せず、
  推論コストを平均 20% 以上増やす」「**指示はよく従われるが、リポジトリ概観は役に立たない**」。
  エージェントが従わないのではなく、従っているのに概観が効かない。
- **[Anthropic: Effective harnesses for long-running agents](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents)** —
  「後続のエージェントが進捗を見て、勝手に完了と宣言する」観測。対策は
  「E2E 検証が通ってから初めて passes を立てる」。**harness の「完了は打ち消し線+✅」様式が
  直撃する罠。**
- **memory bank 系は上流ごと死んだ** — Roo-Code は 2026-05-15 archived、メンテナが
  「Memory banks don't work without significant manual maintenance」と明言。
  `roo-code-memory-bank` の作者本人が md 方式を捨てて MCP+RAG へ移行。
- **agent-os は自作機構を捨てた** — v3 で「spec 執筆は Plan mode、タスク分解は todo list に
  任せる」とし、残したのは standards の索引化と選択的注入だけ = `.claude/rules/` と同じ結論。

**SDD との関係**: 似て非なるもの。SDD の spec は「これから何を作るか」を実装**前**に書く
駆動文書で正典は仕様側にあるが、ND は「今どこにいるか」を作業**中**に書く状態文書で
正典はコード側にある。時制と権威の向きが逆。上記の agent-os の撤退と ETH の結果は、
むしろ**重い spec 層を持つことへの警告**として読む。共通点は「タスクの明文化を人間の記憶では
なくファイルに置く」の一点だけ。

**ドキュメントの置き場の序列**(この序列が設計判断を決める):
①コードの近くにコメント → ②全体像の最小限を repo の docs → ③跨ぐ経緯は Issue/PR コメント。
③が限度。**跨ぎの経緯だけは①②で受けられないので外に出す**(report.sh がこれ)。

## 既知の不具合(2026-08-05 時点)

| # | 箇所 | 内容 |
|---|---|---|
| 1 | `assets/session-start.sh` | 鮮度検査の正規表現が日付直後の閉じ括弧を要求。`(2026-08-05・第1版)` にマッチせず2リポジトリで死んでいる |
| 2 | `scripts/survey.sh` | 世代検出が `.claude/rules/harness.md` の説明文を拾い `harness-template v (未導入)` という矛盾出力 |
| 3 | `scripts/install.sh` | `core.hooksPath` を無条件上書き。既存 hooksPath を持つリポジトリで既存フックを黙って無効化 |
| 4 | `assets/comments-rule.md` | `paths:` 付きのため**コンパクションで消える**。最も効いてほしい長時間の実装作業中に失われる。核を CLAUDE.md 側に置く二段構えが要る |

## subagent への到達範囲(運用上の帰結)

- custom subagent(implementer / artisan)には CLAUDE.md 階層と project rules が**届く**
  → `comments.md` は実装役に効いている。
- **SessionStart フックの注入は subagent には入らない。**
- 公式ドキュメント上、**built-in の Explore と Plan は CLAUDE.md すら読まない**。
  → Explore/Plan に調べさせるときは**正典のパスをプロンプトに明記する**必要がある。

## 設計の根拠(敵対的検証で確定したもの)

v0.1.0 に対し観点を分けた3体(実行系を実際に壊す / 公式仕様へ接地 / 設計レッドチーム)で
敵対的検証を実施し、21指摘のうち14を採用。**深刻度「高」の指摘2件は偽陽性**で、公式
ドキュメントの原文引用と本番実績で棄却している(指摘は裁定するもので、従うものではない)。
方法は `plugins/harness/skills/doctor/references/adversarial-verification.md`。

- **fail-closed**: マーカーが無いとき全文注入へフォールバックせず警告して止まる。
- **行頭アンカー**: 散文中で `session-head-end` に言及しただけで頭が切断される誤爆を防ぐ。
- **計測は寛容に、教える書式は厳密に**: 書式が少しズレただけで腐敗検知が静かに死ぬ設計にしない。
- **鮮度検査**: 現在地の日付 vs **リポジトリ全体の**最終コミット日。⚠️ **コンポーネント別に
  見ていない**(`check.sh:505` が `git log -1` をパス制限なしで呼ぶ)ので、harness だけ触った
  日に ios-skills の正典まで「古い」と指摘される。tidy 側は 2026-08-06 に直した同型の不具合が
  doctor 側に残っている(→ `H-24`)。
- **CLAUDE.md を置かない**: 常時ロードの層を持たず、規約は `.claude/rules/<component>.md`
  (paths スコープ)、現在地は SessionStart が注入する冒頭に寄せている。正典の冒頭が言う
  「冒頭に二重に置かない」を配布元自身で守った形。**doctor は毎回「CLAUDE.md が無い」と
  指摘するが、これは意図的な省略。**この行が記録 —— 無いと次のセッションが再発見して再判断する。
- **閾値の上げ方向調整を禁止**: 警告を消す最小コスト行動が閾値引き上げにならないように。
- **バージョン刻印**: 「そのファイルの内容が最後に変わった版」。ファイルごとに違うのが正常。

未解決の指摘: 運用契約の強制機構は無い(検知器のみ)。チーム共有リポでは
`.claude/settings.json` + フック .sh のコミットが「clone した全員のセッション開始時に
実行されるコード」になる(個人リポ前提と注意書きしてあるだけ)。

## skill 構成(cclens に倣った整理)

**動詞で分ける。実装で分けない。** cclens の doctor(診る)/ optimize(直す)/ query(訊く)は
同じデータへの違う意図なので重複しない。当初 audit(手順書)と doctor(検査)に分けたが
どちらも「診る」で冗長だったため 2026-08-05 に doctor へ統合した。

- **`/harness:doctor`**(診る + 入れる) — check.sh(`!` で自動実行・検査)+ install.sh(破壊的・承認後)
  + survey.sh(導入判断の材料)+ calibrate.sh(較正)+ report.sh(検査が外れたら起票)

**SKILL.md は薄く保ち、ロジックはスクリプトへ**(cclens の各 skill は 49〜60 行)。
doctor は導入手順を `references/installing.md` へ progressive disclosure 済み。

## 抜け漏れ点検(2026-08-05・実物比較)

推測ではなく caldav/swift-mcp-app の実物と比較して4件見つけ、install.sh に取り込んだ:
`.gitignore` の `.claude/settings.local.json`(store-redirect で漏れ寸前だった)、
`docs/log.md`(`--with-log`)、`.codex/README.md`(保守規約)、`.agents/skills/*` symlink。

## この repo 固有の事情

- **マーケットプレイス型なので単一の「現在地」が無い。** フックは全文注入ではなく
  `docs/<component>/next-directions.md` への軽量 pointer(2026-08-02 の判断)。
  **コンポーネント = ND を持つディレクトリ**という定義は `docs/principles.md` 規則4 が正典。
  レジストリは作らない(既存機構がもう定義を持っているため)。
- ~~pre-push 用の検証コマンドが無い~~ ✅ 2026-08-08 導入。`scripts/verify.sh`(シェル構文 /
  JSON 妥当性 / 正典の書式)を `.githooks/pre-push` から呼ぶ。**`core.hooksPath` は
  `.git/config` に入るので clone ごとに1回 `git config core.hooksPath .githooks` が要る。**

### doctor の指摘のうち、このリポジトリでは却下するもの

**⚠️ 却下は理由を書かないと、次のセッションが同じ指摘を再発見して再判断する**
(`doctor/SKILL.md` の Operating Posture)。ここがその記録。

- **「CLAUDE.md が無い」→ 却下。**複数プラグインの寄せ集めなので、**全プラグインに常時
  ロードすべき横断の事実がほとんど無い。**関心は `.claude/rules/<component>.md` の
  `paths:` 付きに寄せてあり、触っている間だけ生きる形になっている(規則1「情報は寿命で
  置き場を決める」)。CLAUDE.md を作れば**サブエージェント起動回数ぶん乗算で払う**ものが
  増えるだけで、届く先が広がらない。**この構成が変わる(横断の常時ルールが実際に要る)まで
  再検討しない。**
- **`--lint` の `[head-costly]`(冒頭が予算超過)→ 保留。**この警告は**冒頭が毎セッション
  注入される前提**で書かれているが、このリポジトリのフックは pointer 方式なので**冒頭は
  注入されていない。**つまり指摘の前提がここでは偽。ただし冒頭が肥大していること自体は
  事実(`H-5` の整理対象)なので、警告そのものは消さない。検査側を pointer 方式に
  対応させるかは `H-22` と同型の判断になるため、そちらと合わせて決める。
