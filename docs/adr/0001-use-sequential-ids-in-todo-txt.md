# todo.txt の id は連番にする

Date: 2026-09-10

並行 worktree での採番衝突を避けるため乱数 id（6 桁 16 進）を一度実装したが、todo.txt を書くのは本線だけで、worktree の implementer は報告するだけ、と運用を決めたので書き手は一つになり、衝突は起きない。書き手が一つなら連番で足り、人が読めて短く指せる。`id:0012` の 4 桁ゼロ詰めで ADR の番号と揃える。
