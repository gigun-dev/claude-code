# 決定の索引を `todo ready` の出力に載せる

Date: 2026-09-10
Implementation: done

ADR を読む経路がスキルの裁量任せだと、読まれずに決定が黙って破られる(最初に破られたのは 0001 だった)。hook は 0002 で退けてあるので、必ず打つ `todo ready` の出力に `adr ls` と同じ題の索引を載せる(本文は出さず truncate もしない)。索引が閾値(既定 30 行)を超えたら索引自身が 1 行報せる。
